<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "81d087662fb3dd7b7124bce1a9c9ec86",
  "translation_date": "2026-01-05T21:41:43+00:00",
  "source_file": "03-rag/README.md",
  "language_code": "fa"
}
-->
# ماژول ۰۳: RAG (تولید تقویت‌شده با بازیابی)

## فهرست مطالب

- [آنچه خواهید آموخت](../../../03-rag)
- [پیش‌نیازها](../../../03-rag)
- [درک RAG](../../../03-rag)
- [نحوه کارکرد](../../../03-rag)
  - [پردازش سند](../../../03-rag)
  - [ایجاد جاسازی‌ها](../../../03-rag)
  - [جستجوی معنایی](../../../03-rag)
  - [تولید پاسخ](../../../03-rag)
- [اجرای برنامه](../../../03-rag)
- [استفاده از برنامه](../../../03-rag)
  - [بارگذاری سند](../../../03-rag)
  - [پرسیدن سوال](../../../03-rag)
  - [بررسی مراجع منبع](../../../03-rag)
  - [آزمایش با سوالات](../../../03-rag)
- [مفاهیم کلیدی](../../../03-rag)
  - [استراتژی تقسیم‌بندی](../../../03-rag)
  - [امتیازهای شباهت](../../../03-rag)
  - [ذخیره‌سازی در حافظه](../../../03-rag)
  - [مدیریت پنجرهٔ زمینه](../../../03-rag)
- [زمان اهمیت RAG](../../../03-rag)
- [گام‌های بعدی](../../../03-rag)

## آنچه خواهید آموخت

در ماژول‌های قبلی، یاد گرفتید چگونه با هوش مصنوعی گفتگو کنید و پرامپت‌های خود را به طور مؤثر ساختاربندی کنید. اما یک محدودیت اساسی وجود دارد: مدل‌های زبانی فقط آنچه را در طول آموزش یاد گرفته‌اند می‌دانند. آن‌ها نمی‌توانند به سوالاتی دربارهٔ سیاست‌های شرکت شما، مستندات پروژه‌تان، یا هر اطلاعاتی که آموزش ندیده‌اند پاسخ دهند.

RAG (تولید تقویت‌شده با بازیابی) این مشکل را حل می‌کند. به جای تلاش برای آموزش مدل با اطلاعات شما (که پرهزینه و غیرعملی است)، به آن امکان می‌دهید تا از طریق اسناد شما جستجو کند. وقتی کسی سوالی می‌پرسد، سیستم اطلاعات مرتبط را پیدا کرده و آن را در پرامپت قرار می‌دهد. سپس مدل بر اساس آن زمینه بازیابی شده پاسخ می‌دهد.

RAG را می‌توان مانند دادن یک کتابخانه مرجع به مدل تصور کرد. وقتی سوال می‌پرسید، سیستم:

1. **پرسش کاربر** - شما سوال می‌پرسید  
2. **جاسازی** - سوال شما را به یک بردار تبدیل می‌کند  
3. **جستجوی برداری** - بخش‌های سند مشابه را پیدا می‌کند  
4. **گردآوری زمینه** - بخش‌های مرتبط را به پرامپت اضافه می‌کند  
5. **پاسخ** - مدل زبانی بزرگ بر اساس آن زمینه پاسخ می‌دهد

این باعث می‌شود پاسخ‌های مدل بر اساس داده‌های واقعی شما باشد نه صرفاً دانش آموزش یا حدس زدن پاسخ‌ها.

<img src="../../../translated_images/rag-architecture.ccb53b71a6ce407f.fa.png" alt="معماری RAG" width="800"/>

*روند کاری RAG - از پرسش کاربر تا جستجوی معنایی و تولید پاسخ متنی*

## پیش‌نیازها

- اتمام ماژول ۰۱ (منابع Azure OpenAI مستقر شده)
- فایل `.env` در دایرکتوری ریشه با اطلاعات اعتبار Azure (ایجاد شده توسط `azd up` در ماژول ۰۱)

> **نکته:** اگر ماژول ۰۱ را کامل نکرده‌اید، ابتدا دستورالعمل‌های استقرار آنجا را دنبال کنید.

## نحوه کارکرد

### پردازش سند

[DocumentService.java](../../../03-rag/src/main/java/com/example/langchain4j/rag/service/DocumentService.java)

وقتی سندی بارگذاری می‌کنید، سیستم آن را به قطعات کوچکتر تقسیم می‌کند — بخش‌هایی کوچک که به راحتی در پنجرهٔ زمینه مدل جا می‌شوند. این بخش‌ها کمی روی همدیگر همپوشانی دارند تا در مرزها زمینه از دست نرود.

```java
Document document = FileSystemDocumentLoader.loadDocument("sample-document.txt");

DocumentSplitter splitter = DocumentSplitters
    .recursive(300, 30, new OpenAiTokenizer());

List<TextSegment> segments = splitter.split(document);
```

> **🤖 امتحان کنید با [GitHub Copilot](https://github.com/features/copilot) چت:** فایل [`DocumentService.java`](../../../03-rag/src/main/java/com/example/langchain4j/rag/service/DocumentService.java) را باز کنید و بپرسید:  
> - "چگونه LangChain4j اسناد را به بخش تقسیم می‌کند و چرا همپوشانی مهم است؟"  
> - "اندازه بهینه بخش برای انواع مختلف سند چقدر است و چرا؟"  
> - "چگونه با اسناد چندزبانه یا با قالب‌بندی خاص برخورد کنم؟"

### ایجاد جاسازی‌ها

[LangChainRagConfig.java](../../../03-rag/src/main/java/com/example/langchain4j/rag/config/LangChainRagConfig.java)

هر بخش به نمایش عددی به نام جاسازی تبدیل می‌شود — اساساً یک اثر انگشت ریاضی که معنای متن را ثبت می‌کند. متن‌های مشابه، جاسازی‌های مشابهی تولید می‌کنند.

```java
@Bean
public EmbeddingModel embeddingModel() {
    return OpenAiOfficialEmbeddingModel.builder()
        .baseUrl(azureOpenAiEndpoint)
        .apiKey(azureOpenAiKey)
        .modelName(azureEmbeddingDeploymentName)
        .build();
}

EmbeddingStore<TextSegment> embeddingStore = 
    new InMemoryEmbeddingStore<>();
```

<img src="../../../translated_images/vector-embeddings.2ef7bdddac79a327.fa.png" alt="فضای جاسازی برداری" width="800"/>

*اسناد به صورت بردارهایی در فضای جاسازی نمایش داده شده‌اند - محتواهای مشابه در کنار هم گروه‌بندی شده‌اند*

### جستجوی معنایی

[RagService.java](../../../03-rag/src/main/java/com/example/langchain4j/rag/service/RagService.java)

وقتی سوال می‌پرسید، سوال شما هم به یک جاسازی تبدیل می‌شود. سیستم جاسازی سوال شما را با جاسازی‌های همه بخش‌های سند مقایسه می‌کند. بخش‌هایی که بیشترین شباهت معنایی را دارند پیدا می‌شوند — نه فقط کلمات کلیدی مطابقت یافته، بلکه شبیه‌سازی معنایی واقعی.

```java
Embedding queryEmbedding = embeddingModel.embed(question).content();

List<EmbeddingMatch<TextSegment>> matches = 
    embeddingStore.findRelevant(queryEmbedding, 5, 0.7);

for (EmbeddingMatch<TextSegment> match : matches) {
    String relevantText = match.embedded().text();
    double score = match.score();
}
```

> **🤖 امتحان کنید با [GitHub Copilot](https://github.com/features/copilot) چت:** فایل [`RagService.java`](../../../03-rag/src/main/java/com/example/langchain4j/rag/service/RagService.java) را باز کنید و بپرسید:  
> - "جستجوی شباهت با جاسازی‌ها چگونه کار می‌کند و امتیاز چگونه تعیین می‌شود؟"  
> - "چه آستانه شباهتی باید استفاده کنم و این چگونه نتایج را تحت تاثیر قرار می‌دهد؟"  
> - "چگونه مواردی که هیچ سند مرتبطی پیدا نمی‌شود را مدیریت کنم؟"

### تولید پاسخ

[RagService.java](../../../03-rag/src/main/java/com/example/langchain4j/rag/service/RagService.java)

مربوط‌ترین بخش‌ها در پرامپت به مدل درج می‌شوند. مدل آن بخش‌های مشخص را می‌خواند و بر اساس آن به سوال شما پاسخ می‌دهد. این از توهم‌سازی جلوگیری می‌کند — مدل فقط می‌تواند از آنچه روبروی خود دارد پاسخ دهد.

## اجرای برنامه

**تأیید استقرار:**

اطمینان حاصل کنید فایل `.env` در دایرکتوری ریشه با اعتبار Azure موجود است (در ماژول ۰۱ ایجاد شده است):  
```bash
cat ../.env  # باید AZURE_OPENAI_ENDPOINT، API_KEY، DEPLOYMENT را نشان دهد
```
  
**شروع برنامه:**

> **نکته:** اگر قبلاً همه برنامه‌ها را با `./start-all.sh` از ماژول ۰۱ شروع کرده‌اید، این ماژول در پورت ۸۰۸۱ در حال اجراست. می‌توانید دستورات شروع زیر را رد کنید و مستقیماً به http://localhost:8081 بروید.

**گزینه ۱: استفاده از Spring Boot Dashboard (پیشنهاد برای کاربران VS Code)**

کانتینر توسعه شامل افزونه Spring Boot Dashboard است که یک رابط بصری برای مدیریت همه برنامه‌های Spring Boot ارائه می‌دهد. می‌توانید آن را در نوار فعالیت سمت چپ VS Code (نماد Spring Boot) پیدا کنید.

از طریق Spring Boot Dashboard می‌توانید:  
- همه برنامه‌های Spring Boot موجود در محیط کاری را ببینید  
- با یک کلیک برنامه‌ها را شروع/توقف کنید  
- لاگ‌های برنامه را به صورت زنده مشاهده کنید  
- وضعیت برنامه را مانیتور کنید  

فقط کافی است دکمه پخش کنار "rag" را بفشارید تا این ماژول شروع شود، یا همه ماژول‌ها را همزمان اجرا کنید.

<img src="../../../translated_images/dashboard.fbe6e28bf4267ffe.fa.png" alt="داشبورد Spring Boot" width="400"/>

**گزینه ۲: استفاده از اسکریپت‌های شِل**

اجرای همه برنامه‌های وب (ماژول‌های ۰۱-۰۴):

**Bash:**  
```bash
cd ..  # از دایرکتوری ریشه
./start-all.sh
```
  
**PowerShell:**  
```powershell
cd ..  # از فهرست اصلی
.\start-all.ps1
```
  
یا فقط این ماژول را اجرا کنید:

**Bash:**  
```bash
cd 03-rag
./start.sh
```
  
**PowerShell:**  
```powershell
cd 03-rag
.\start.ps1
```
  
هر دو اسکریپت به‌طور خودکار متغیرهای محیطی از فایل `.env` ریشه را بارگذاری کرده و در صورت نبود فایل‌های JAR آن‌ها را می‌سازند.

> **نکته:** اگر ترجیح می‌دهید قبل از شروع همه ماژول‌ها را به‌صورت دستی بسازید:  
>  
> **Bash:**  
> ```bash
> cd ..  # Go to root directory
> mvn clean package -DskipTests
> ```
  
> **PowerShell:**  
> ```powershell
> cd ..  # Go to root directory
> mvn clean package -DskipTests
> ```
  
مرورگر را باز کرده و به http://localhost:8081 بروید.

**برای توقف:**  

**Bash:**  
```bash
./stop.sh  # فقط این ماژول
# یا
cd .. && ./stop-all.sh  # همه ماژول‌ها
```
  
**PowerShell:**  
```powershell
.\stop.ps1  # فقط این ماژول
# یا
cd ..; .\stop-all.ps1  # همه ماژول‌ها
```
  
## استفاده از برنامه

برنامه یک رابط وب برای بارگذاری سند و پرسش فراهم می‌کند.

<a href="images/rag-homepage.png"><img src="../../../translated_images/rag-homepage.d90eb5ce1b3caa94.fa.png" alt="رابط برنامه RAG" width="800" style="border: 1px solid #ddd; box-shadow: 0 2px 8px rgba(0,0,0,0.1);"/></a>

*رابط برنامه RAG - بارگذاری مستند و پرسیدن سوال*

### بارگذاری سند

با بارگذاری یک سند شروع کنید — فایل‌های TXT برای تست بهترین هستند. یک فایل `sample-document.txt` در این دایرکتوری موجود است که حاوی اطلاعات درباره ویژگی‌های LangChain4j، پیاده‌سازی RAG و بهترین شیوه‌ها است — مناسب برای تست سیستم.

سیستم سند شما را پردازش کرده، به بخش تقسیم می‌کند و برای هر بخش جاسازی ایجاد می‌کند. این فرایند به‌صورت خودکار هنگام بارگذاری رخ می‌دهد.

### پرسیدن سوال

اکنون سوالات مشخصی درباره محتوای سند بپرسید. سعی کنید سوال‌های واقعی که به وضوح در سند بیان شده‌اند را امتحان کنید. سیستم بخش‌های مرتبط را جستجو کرده، آن‌ها را در پرامپت وارد می‌کند و پاسخ تولید می‌کند.

### بررسی مراجع منبع

توجه داشته باشید هر پاسخ حاوی مراجع منبع به همراه امتیازهای شباهت است. این امتیازها (۰ تا ۱) نشان می‌دهند هر بخش چقدر به سوال شما مرتبط بوده است. نمرات بالاتر نشان‌دهنده تطابق بهتر است. این به شما امکان می‌دهد پاسخ را با منبع اصلی بررسی کنید.

<a href="images/rag-query-results.png"><img src="../../../translated_images/rag-query-results.6d69fcec5397f355.fa.png" alt="نتایج پرسش RAG" width="800" style="border: 1px solid #ddd; box-shadow: 0 2px 8px rgba(0,0,0,0.1);"/></a>

*نتایج پرسش که پاسخ را همراه با مراجع منبع و امتیازهای ارتباط نمایش می‌دهد*

### آزمایش با سوالات

انواع مختلف سوالات را امتحان کنید:  
- حقایق مشخص: "موضوع اصلی چیست؟"  
- مقایسه‌ها: "تفاوت X و Y چیست؟"  
- خلاصه‌ها: "نقاط کلیدی درباره Z را خلاصه کن"

ببینید امتیازهای ارتباط چطور بر اساس میزان تطابق سوال شما با محتوای سند تغییر می‌کند.

## مفاهیم کلیدی

### استراتژی تقسیم‌بندی

اسناد به بخش‌های ۳۰۰ توکنی با همپوشانی ۳۰ توکنی تقسیم می‌شوند. این تعادل اطمینان می‌دهد هر بخش دارای زمینه کافی برای مفهوم‌دار بودن است در حالی که به اندازه‌ای کوچک است که بتوان چندین بخش را در یک پرامپت گنجاند.

### امتیازهای شباهت

امتیازها بین ۰ تا ۱ هستند:  
- ۰.۷-۱.۰: بسیار مرتبط، تطابق دقیق  
- ۰.۵-۰.۷: مرتبط، زمینه خوب  
- زیر ۰.۵: حذف شده، خیلی نامرتبط  

سیستم تنها بخش‌هایی که بالاتر از حداقل آستانه باشند را بازیابی می‌کند تا کیفیت تضمین شود.

### ذخیره‌سازی در حافظه

این ماژول برای سادگی از ذخیره‌سازی در حافظه استفاده می‌کند. وقتی برنامه را ری‌استارت می‌کنید، اسناد بارگذاری شده از دست می‌روند. سیستم‌های تولیدی از پایگاه‌های داده برداری ماندگار مانند Qdrant یا Azure AI Search استفاده می‌کنند.

### مدیریت پنجرهٔ زمینه

هر مدل یک پنجره زمینه حداکثر دارد. نمی‌توانید همه بخش‌های یک سند بزرگ را وارد کنید. سیستم بهترین N بخش مرتبط (پیش‌فرض ۵) را بازیابی می‌کند تا در محدودیت‌ها بماند و زمینه کافی برای پاسخ‌های دقیق فراهم کند.

## زمان اهمیت RAG

**زمان استفاده از RAG:**  
- پاسخ به سوالات درباره اسناد اختصاصی  
- اطلاعاتی که به‌طور مکرر تغییر می‌کنند (سیاست‌ها، قیمت‌ها، مشخصات)  
- دقت نیازمند استناد به منبع  
- محتوا برای گنجاندن در یک پرامپت بسیار بزرگ است  
- نیاز به پاسخ‌های قابل تایید و مبتنی بر داده واقعی است

**زمان عدم استفاده از RAG:**  
- سوالات به دانش عمومی مدل نیاز دارند  
- نیاز به داده‌های زمان واقعی است (RAG فقط روی اسناد بارگذاری شده کار می‌کند)  
- محتوا به اندازه‌ای کوچک است که مستقیماً در پرامپت موجود باشد

## گام‌های بعدی

**ماژول بعدی:** [۰۴-tools - عامل‌های هوش مصنوعی با ابزارها](../04-tools/README.md)

---

**ناوبری:** [← قبلی: ماژول ۰۲ - مهندسی پرامپت](../02-prompt-engineering/README.md) | [بازگشت به اصلی](../README.md) | [بعدی: ماژول ۰۴ - ابزارها →](../04-tools/README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**توضیح مهم**:  
این سند با استفاده از سرویس ترجمه هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما در دقت ترجمه تلاش می‌کنیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است حاوی خطاها یا نادرستی‌هایی باشند. سند اصلی به زبان بومی خود، منبع معتبر و اصلی محسوب می‌شود. برای اطلاعات حیاتی، ترجمه حرفه‌ای توسط انسان توصیه می‌گردد. ما در قبال هرگونه سوء تفاهم یا تفسیر نادرست ناشی از استفاده از این ترجمه مسئولیتی نداریم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->