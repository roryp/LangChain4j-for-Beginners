<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "f89f4c106d110e4943c055dd1a2f1dff",
  "translation_date": "2025-12-30T20:29:02+00:00",
  "source_file": "05-mcp/README.md",
  "language_code": "fa"
}
-->
# ماژول 05: پروتکل زمینه مدل (MCP)

## فهرست مطالب

- [آنچه خواهید آموخت](../../../05-mcp)
- [MCP چیست؟](../../../05-mcp)
- [MCP چگونه کار می‌کند؟](../../../05-mcp)
- [ماژول عامل‌محور](../../../05-mcp)
- [اجرای نمونه‌ها](../../../05-mcp)
  - [پیش‌نیازها](../../../05-mcp)
- [شروع سریع](../../../05-mcp)
  - [عملیات فایل (Stdio)](../../../05-mcp)
  - [عامل ناظر](../../../05-mcp)
    - [درک خروجی](../../../05-mcp)
    - [توضیح ویژگی‌های ماژول عامل‌محور](../../../05-mcp)
- [مفاهیم کلیدی](../../../05-mcp)
- [تبریک!](../../../05-mcp)
  - [گام بعدی؟](../../../05-mcp)

## آنچه خواهید آموخت

شما یک هوش مصنوعی مکالمه‌ای ساختید، در مهندسی پرامپت‌ها خبره شدید، پاسخ‌ها را در اسناد زمینه‌سازی کردید و عامل‌هایی با ابزارهای سفارشی ساختید. اما تمام آن ابزارها برای برنامهٔ خاص شما سفارشی‌سازی شده بودند. چه می‌شد اگر می‌توانستید به هوش مصنوعی خود دسترسی به یک اکوسیستم استاندارد از ابزارها بدهید که هر کسی بتواند آن‌ها را بسازد و به اشتراک بگذارد؟ در این ماژول، خواهید آموخت چگونه دقیقاً این کار را با پروتکل زمینه مدل (MCP) و ماژول عامل‌محور LangChain4j انجام دهید. ابتدا یک خوانندهٔ فایل سادهٔ MCP را نشان می‌دهیم و سپس چگونگی ادغام آسان آن در جریان‌های کاری عامل‌محور پیشرفته با استفاده از الگوی عامل ناظر را نمایش می‌دهیم.

## MCP چیست؟

پروتکل زمینه مدل (MCP) دقیقاً همین را فراهم می‌کند — روشی استاندارد برای کشف و استفادهٔ برنامه‌های هوش مصنوعی از ابزارهای خارجی. به جای نوشتن یکپارچه‌سازی‌های سفارشی برای هر منبع داده یا سرویس، به سرورهای MCP متصل می‌شوید که قابلیت‌های خود را به‌صورت قالبی یکسان ارائه می‌دهند. عامل هوش مصنوعی شما سپس می‌تواند این ابزارها را به‌صورت خودکار کشف و استفاده کند.

<img src="../../../translated_images/mcp-comparison.9129a881ecf10ff5.fa.png" alt="مقایسهٔ MCP" width="800"/>

*قبل از MCP: یکپارچه‌سازی‌های نقطه‌به‌نقطه پیچیده. بعد از MCP: یک پروتکل، امکانات بی‌پایان.*

MCP یک مشکل بنیادی در توسعهٔ هوش مصنوعی را حل می‌کند: هر یکپارچه‌سازی سفارشی است. می‌خواهید به GitHub دسترسی پیدا کنید؟ کد سفارشی. می‌خواهید فایل‌ها را بخوانید؟ کد سفارشی. می‌خواهید از یک پایگاه‌داده پرس‌وجو کنید؟ کد سفارشی. و هیچ‌یک از این یکپارچه‌سازی‌ها با برنامه‌های دیگر هوش مصنوعی کار نمی‌کنند.

MCP این را استاندارد می‌کند. یک سرور MCP ابزارها را با توضیحات و اسکیمای پارامترها در معرض قرار می‌دهد. هر کلاینت MCP می‌تواند متصل شود، ابزارهای موجود را کشف کند و از آن‌ها استفاده کند. یک بار بسازید، همه‌جا استفاده کنید.

<img src="../../../translated_images/mcp-architecture.b3156d787a4ceac9.fa.png" alt="معماری MCP" width="800"/>

*معماری پروتکل زمینه مدل - کشف و اجرای ابزارها به‌صورت استاندارد*

## MCP چگونه کار می‌کند؟

**معماری سرور-کلاینت**

MCP از مدل کلاینت-سرور استفاده می‌کند. سرورها ابزارها را فراهم می‌کنند — خواندن فایل‌ها، پرس‌وجوی پایگاه‌داده، فراخوانی APIها. کلاینت‌ها (برنامهٔ هوش مصنوعی شما) به سرورها متصل می‌شوند و از ابزارهای آن‌ها استفاده می‌کنند.

برای استفاده از MCP با LangChain4j، این وابستگی Maven را اضافه کنید:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-mcp</artifactId>
    <version>${langchain4j.version}</version>
</dependency>
```

**کشف ابزار**

وقتی کلاینت شما به یک سرور MCP متصل می‌شود، می‌پرسد «چه ابزارهایی دارید؟» سرور با فهرستی از ابزارهای در دسترس پاسخ می‌دهد، هر کدام با توضیحات و اسکیمای پارامترها. عامل هوش مصنوعی شما سپس می‌تواند بر اساس درخواست کاربر تصمیم بگیرد کدام ابزارها را استفاده کند.

**مکانیزم‌های انتقال**

MCP از مکانیزم‌های انتقال مختلف پشتیبانی می‌کند. این ماژول انتقال Stdio را برای فرآیندهای محلی نشان می‌دهد:

<img src="../../../translated_images/transport-mechanisms.2791ba7ee93cf020.fa.png" alt="مکانیزم‌های انتقال" width="800"/>

*مکانیزم‌های انتقال MCP: HTTP برای سرورهای راه‌دور، Stdio برای فرآیندهای محلی*

**Stdio** - [StdioTransportDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java)

برای فرآیندهای محلی. برنامهٔ شما یک سرور را به‌عنوان یک زیرفرآیند ایجاد می‌کند و از طریق ورودی/خروجی استاندارد با آن ارتباط برقرار می‌کند. مفید برای دسترسی به سیستم فایل یا ابزارهای خط‌فرمان.

```java
McpTransport stdioTransport = new StdioMcpTransport.Builder()
    .command(List.of(
        npmCmd, "exec",
        "@modelcontextprotocol/server-filesystem@2025.12.18",
        resourcesDir
    ))
    .logEvents(false)
    .build();
```

> **🤖 امتحان با [GitHub Copilot](https://github.com/features/copilot) Chat:** فایل [`StdioTransportDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java) را باز کنید و بپرسید:
> - «انتقال Stdio چگونه کار می‌کند و کی باید از آن در مقابل HTTP استفاده کنم؟»
> - «LangChain4j چرخهٔ حیات فرایندهای سرور MCP ایجادشده را چگونه مدیریت می‌کند؟»
> - «مفاهیم امنیتی دسترسی هوش مصنوعی به سیستم فایل چیست؟»

## ماژول عامل‌محور

در حالی که MCP ابزارهای استانداردی فراهم می‌کند، ماژول عامل‌محور LangChain4j یک روش اعلامی برای ساخت عامل‌هایی ارائه می‌دهد که آن ابزارها را ارکسترا می‌کنند. یادداشت‌گذاری `@Agent` و `AgenticServices` به شما اجازه می‌دهد رفتار عامل را از طریق اینترفیس‌ها تعریف کنید تا کد امری.

در این ماژول، الگوی **عامل ناظر (Supervisor Agent)** را بررسی خواهید کرد — یک رویکرد پیشرفتهٔ عامل‌محور که در آن یک عامل «ناظر» به‌طور پویا تصمیم می‌گیرد بر اساس درخواست کاربر کدام زیرعامل‌ها را فراخوانی کند. ما هر دو مفهوم را با دادن توانایی دسترسی فایل مبتنی بر MCP به یکی از زیرعامل‌ها ترکیب می‌کنیم.

برای استفاده از ماژول عامل‌محور، این وابستگی Maven را اضافه کنید:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-agentic</artifactId>
    <version>${langchain4j.mcp.version}</version>
</dependency>
```

> **⚠️ آزمایشی:** ماژول `langchain4j-agentic` در وضعیت **آزمایشی** است و ممکن است تغییر کند. روش پایدار برای ساخت دستیارهای هوش مصنوعی همچنان `langchain4j-core` با ابزارهای سفارشی است (ماژول 04).

## اجرای نمونه‌ها

### پیش‌نیازها

- Java 21+، Maven 3.9+
- Node.js 16+ و npm (برای سرورهای MCP)
- متغیرهای محیطی پیکربندی‌شده در فایل `.env` (از دایرکتوری ریشه):
  - **برای StdioTransportDemo:** `GITHUB_TOKEN` (توکن دسترسی شخصی GitHub)
  - **برای SupervisorAgentDemo:** `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_DEPLOYMENT` (همانند ماژول‌های 01-04)

> **توجه:** اگر هنوز متغیرهای محیطی خود را تنظیم نکرده‌اید، دستورالعمل‌ها را در [Module 00 - Quick Start](../00-quick-start/README.md) ببینید، یا `.env.example` را به `.env` در دایرکتوری ریشه کپی کنید و مقادیر خود را وارد کنید.

## شروع سریع

**استفاده از VS Code:** به‌سادگی روی هر فایل دموی دلخواه در اکسپلورر راست‌کلیک کنید و **"Run Java"** را انتخاب کنید، یا از پیکربندی‌های لانچ در پنل Run and Debug استفاده کنید (مطمئن شوید که ابتدا توکن خود را به فایل `.env` اضافه کرده‌اید).

**استفاده از Maven:** به‌علاوه، می‌توانید با اجرای دستورات زیر از خط فرمان نمونه‌ها را اجرا کنید.

### عملیات فایل (Stdio)

این نمونه ابزارهای مبتنی بر زیرفرآیند محلی را نشان می‌دهد.

**✅ هیچ پیش‌نیازی لازم نیست** - سرور MCP به‌صورت خودکار اجرا می‌شود.

**استفاده از VS Code:** روی `StdioTransportDemo.java` راست‌کلیک کرده و **"Run Java"** را انتخاب کنید.

**استفاده از Maven:**

**Bash:**
```bash
export GITHUB_TOKEN=your_token_here
cd 05-mcp
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.mcp.StdioTransportDemo
```

**PowerShell:**
```powershell
$env:GITHUB_TOKEN=your_token_here
cd 05-mcp
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.mcp.StdioTransportDemo
```

برنامه به‌صورت خودکار یک سرور MCP برای سیستم فایل ایجاد می‌کند و یک فایل محلی را می‌خواند. توجه کنید مدیریت زیرفرآیند برای شما انجام می‌شود.

**خروجی مورد انتظار:**
```
Assistant response: The file provides an overview of LangChain4j, an open-source Java library
for integrating Large Language Models (LLMs) into Java applications...
```

### عامل ناظر

<img src="../../../translated_images/agentic.cf84dcda226374e3.fa.png" alt="ماژول عامل‌محور" width="800"/>


الگوی **عامل ناظر** شکلی **انعطاف‌پذیر** از عامل‌محوری است. بر خلاف جریان‌های کاری قطعی (ترتیبی، حلقه، موازی)، یک ناظر از یک LLM استفاده می‌کند تا به‌طور خودکار تصمیم بگیرد بر اساس درخواست کاربر کدام عامل‌ها را فراخوانی کند.

**ترکیب ناظر با MCP:** در این مثال، به `FileAgent` دسترسی به ابزارهای سیستم فایل مبتنی بر MCP از طریق `toolProvider(mcpToolProvider)` دادیم. وقتی کاربر درخواست «خواندن و تحلیل یک فایل» می‌دهد، ناظر درخواست را تحلیل کرده و یک برنامهٔ اجرایی تولید می‌کند. سپس درخواست را به `FileAgent` هدایت می‌کند که از ابزار `read_file` در MCP برای بازیابی محتوا استفاده می‌کند. ناظر آن محتوا را به `AnalysisAgent` برای تفسیر منتقل می‌کند و در صورت نیاز `SummaryAgent` را برای خلاصه‌سازی نتایج فراخوانی می‌کند.

این نشان می‌دهد که ابزارهای MCP چگونه به‌راحتی در جریان‌های کاری عامل‌محور ادغام می‌شوند — ناظر نیازی به دانستنِ «چگونه» خواندن فایل‌ها ندارد، فقط می‌داند که `FileAgent` می‌تواند این کار را انجام دهد. ناظر به‌صورت پویا به انواع مختلف درخواست‌ها سازگار می‌شود و یا پاسخ آخرین عامل را بازمی‌گرداند یا خلاصه‌ای از تمام عملیات را.

**استفاده از اسکریپت‌های شروع (توصیه‌شده):**

اسکریپت‌های شروع متغیرهای محیطی را از فایل `.env` ریشه به‌طور خودکار بارگذاری می‌کنند:

**Bash:**
```bash
cd 05-mcp
chmod +x start.sh
./start.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start.ps1
```

**استفاده از VS Code:** روی `SupervisorAgentDemo.java` راست‌کلیک کرده و **"Run Java"** را انتخاب کنید (مطمئن شوید فایل `.env` پیکربندی شده است).

**نحوهٔ کار ناظر:**

```java
// چند عامل با قابلیت‌های مشخص تعریف کنید
FileAgent fileAgent = AgenticServices.agentBuilder(FileAgent.class)
        .chatModel(model)
        .toolProvider(mcpToolProvider)  // دارای ابزارهای MCP برای عملیات فایل است
        .build();

AnalysisAgent analysisAgent = AgenticServices.agentBuilder(AnalysisAgent.class)
        .chatModel(model)
        .build();

SummaryAgent summaryAgent = AgenticServices.agentBuilder(SummaryAgent.class)
        .chatModel(model)
        .build();

// یک Supervisor ایجاد کنید که این عوامل را هماهنگ کند
SupervisorAgent supervisor = AgenticServices.supervisorBuilder()
        .chatModel(model)  // مدل «planner»
        .subAgents(fileAgent, analysisAgent, summaryAgent)
        .responseStrategy(SupervisorResponseStrategy.SUMMARY)
        .build();

// Supervisor به‌طور خودکار تصمیم می‌گیرد کدام عامل‌ها را فراخوانی کند
// فقط یک درخواست به زبان طبیعی ارسال کنید — LLM اجرای آن را برنامه‌ریزی می‌کند
String response = supervisor.invoke("Read the file at /path/file.txt and analyze it");
```

برای پیاده‌سازی کامل به [SupervisorAgentDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) مراجعه کنید.

> **🤖 امتحان با [GitHub Copilot](https://github.com/features/copilot) Chat:** فایل [`SupervisorAgentDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) را باز کنید و بپرسید:
> - «ناظر چگونه تصمیم می‌گیرد کدام عامل‌ها را فراخوانی کند؟»
> - «تفاوت میان الگوی ناظر و الگوی جریان کاری ترتیبی چیست؟»
> - «چگونه می‌توان رفتار برنامه‌ریزی ناظر را شخصی‌سازی کرد؟»

#### درک خروجی

وقتی دموی را اجرا می‌کنید، گردش ساختاریافته‌ای از چگونگی ارکستراسیون چند عامل توسط ناظر را می‌بینید. هر بخش چه معنایی دارد:

```
======================================================================
  SUPERVISOR AGENT DEMO
======================================================================

This demo shows how a Supervisor Agent orchestrates multiple specialized agents.
The Supervisor uses an LLM to decide which agent to call based on the task.
```

**سربرگ** معرفی دموی را انجام می‌دهد و مفهوم اصلی را توضیح می‌دهد: ناظر از یک LLM (نه قواعد سخت‌کد شده) برای تصمیم‌گیری دربارهٔ کدام عامل‌ها استفاده می‌کند.

```
--- AVAILABLE AGENTS -------------------------------------------------
  [FILE]     FileAgent     - Reads files using MCP filesystem tools
  [ANALYZE]  AnalysisAgent - Analyzes content for structure, tone, and themes
  [SUMMARY]  SummaryAgent  - Creates concise summaries of content
```

**عامل‌های در دسترس** سه عامل تخصصی را که ناظر می‌تواند از میان آن‌ها انتخاب کند نشان می‌دهد. هر عامل یک قابلیت خاص دارد:
- **FileAgent** می‌تواند فایل‌ها را با استفاده از ابزارهای MCP بخواند (قابلیت خارجی)
- **AnalysisAgent** محتوا را تحلیل می‌کند (قابلیت صرفاً LLM)
- **SummaryAgent** خلاصه‌سازی انجام می‌دهد (قابلیت صرفاً LLM)

```
--- USER REQUEST -----------------------------------------------------
  "Read the file at .../file.txt and analyze what it's about"
```

**درخواست کاربر** نشان می‌دهد چه چیزی خواسته شده است. ناظر باید این را پارس کند و تصمیم بگیرد کدام عامل‌ها را فراخوانی کند.

```
--- SUPERVISOR ORCHESTRATION -----------------------------------------
  The Supervisor will now decide which agents to invoke and in what order...

  +-- STEP 1: Supervisor chose -> FileAgent (reading file via MCP)
  |
  |   Input: .../file.txt
  |
  |   Result: LangChain4j is an open-source Java library designed to simplify...
  +-- [OK] FileAgent (reading file via MCP) completed

  +-- STEP 2: Supervisor chose -> AnalysisAgent (analyzing content)
  |
  |   Input: LangChain4j is an open-source Java library...
  |
  |   Result: Structure: The content is organized into clear paragraphs that int...
  +-- [OK] AnalysisAgent (analyzing content) completed
```

**ارکستراسیون ناظر** جایی است که جادو اتفاق می‌افتد. تماشا کنید چگونه:
1. ناظر **ابتدا FileAgent را انتخاب کرد** زیرا درخواست «خواندن فایل» را ذکر کرده بود
2. FileAgent از ابزار `read_file` در MCP برای بازیابی محتوای فایل استفاده کرد
3. سپس ناظر **AnalysisAgent را انتخاب کرد** و محتوای فایل را به آن ارسال نمود
4. AnalysisAgent ساختار، لحن و تم‌ها را تحلیل کرد

توجه کنید که ناظر این تصمیمات را **به‌طور خودمختار** بر اساس درخواست کاربر اتخاذ کرد — هیچ جریان کاری سخت‌کد شده‌ای وجود ندارد!

**پاسخ نهایی** پاسخ ترکیبی ناظر است که خروجی‌های همهٔ عامل‌هایی که فراخوانی کرده را یکجا می‌کند. مثال دامپِ حوزهٔ عامل‌محور را نشان می‌دهد که خلاصه و نتایج تحلیل ذخیره‌شده توسط هر عامل را نمایش می‌دهد.

```
--- FINAL RESPONSE ---------------------------------------------------
I read the contents of the file and analyzed its structure, tone, and key themes.
The file introduces LangChain4j as an open-source Java library for integrating
large language models...

--- AGENTIC SCOPE (Shared Memory) ------------------------------------
  Agents store their results in a shared scope for other agents to use:
  * summary: LangChain4j is an open-source Java library...
  * analysis: Structure: The content is organized into clear paragraphs that in...
```

### توضیح ویژگی‌های ماژول عامل‌محور

مثال چندین ویژگی پیشرفتهٔ ماژول عامل‌محور را نشان می‌دهد. بیایید نگاهی دقیق‌تر به Agentic Scope و Agent Listeners بیندازیم.

**Agentic Scope** حافظهٔ مشترکی را نشان می‌دهد که عامل‌ها نتایج خود را با استفاده از `@Agent(outputKey="...")` در آن ذخیره کردند. این امکان را می‌دهد:
- عامل‌های بعدی به خروجی‌های عامل‌های قبلی دسترسی داشته باشند
- ناظر پاسخ نهایی را ترکیب کند
- شما خروجی هر عامل را بررسی کنید

```java
ResultWithAgenticScope<String> result = supervisor.invokeWithAgenticScope(request);
AgenticScope scope = result.agenticScope();
String story = scope.readState("story");
List<AgentInvocation> history = scope.agentInvocations("analysisAgent");
```

**Agent Listeners** امکان پایش و اشکال‌زدایی اجرای عامل را فراهم می‌کنند. خروجی گام‌به‌گام که در دمو می‌بینید از یک AgentListener می‌آید که به هر فراخوانی عامل متصل می‌شود:
- **beforeAgentInvocation** - زمانی فراخوانی می‌شود که ناظر عامل را انتخاب می‌کند و به شما اجازه می‌دهد ببینید کدام عامل انتخاب شده و چرا
- **afterAgentInvocation** - زمانی فراخوانی می‌شود که یک عامل کامل می‌شود و نتیجهٔ آن را نمایش می‌دهد
- **inheritedBySubagents** - وقتی true باشد، لیسنر تمام عامل‌ها در سلسله‌مراتب را نظارت می‌کند

```java
AgentListener monitor = new AgentListener() {
    private int step = 0;
    
    @Override
    public void beforeAgentInvocation(AgentRequest request) {
        step++;
        System.out.println("  +-- STEP " + step + ": " + request.agentName());
    }
    
    @Override
    public void afterAgentInvocation(AgentResponse response) {
        System.out.println("  +-- [OK] " + response.agentName() + " completed");
    }
    
    @Override
    public boolean inheritedBySubagents() {
        return true; // به تمام زیرعامل‌ها منتقل کنید
    }
};
```

علاوه بر الگوی ناظر، ماژول `langchain4j-agentic` چندین الگوی جریان کاری و ویژگی قدرتمند دیگر ارائه می‌دهد:

| Pattern | Description | Use Case |
|---------|-------------|----------|
| **Sequential** | اجرای عامل‌ها به‌ترتیب، خروجی به عامل بعدی منتقل می‌شود | خط لوله‌ها: تحقیق → تحلیل → گزارش |
| **Parallel** | اجرای هم‌زمان عامل‌ها | وظایف مستقل: آب و هوا + خبرها + بورس |
| **Loop** | تکرار تا برآورده‌شدن شرط | امتیازدهی کیفیت: تکرار تا زمانی که امتیاز ≥ 0.8 شود |
| **Conditional** | هدایت بر اساس شرایط | طبقه‌بندی → هدایت به عامل متخصص |
| **Human-in-the-Loop** | افزودن نقاط بررسی انسانی | جریان‌های تأیید، بازبینی محتوا |

## مفاهیم کلیدی

**MCP** زمانی ایده‌آل است که بخواهید از اکوسیستم ابزارهای موجود بهره ببرید، ابزارهایی بسازید که چندین برنامه بتوانند به‌اشتراک بگذارند، سرویس‌های شخص ثالث را با پروتکل‌های استاندارد یکپارچه کنید، یا پیاده‌سازی‌های ابزار را بدون تغییر کد جابه‌جا کنید.

**ماژول عامل‌محور** بهترین کارایی را دارد وقتی می‌خواهید تعریف‌های عامل اعلامی با نشانه‌گذاری `@Agent` داشته باشید، نیاز به ارکستراسیون جریان کاری (ترتیبی، حلقه، موازی) دارید، طراحی عامل مبتنی بر اینترفیس را به کد جبری ترجیح می‌دهید، یا چندین عامل را ترکیب می‌کنید که خروجی‌ها را از طریق `outputKey` به اشتراک می‌گذارند.

**الگوی عامل ناظر** زمانی می‌درخشد که جریان کاری قابل‌پیش‌بینی از پیش نباشد و بخواهید LLM تصمیم بگیرد، زمانی که چندین عامل تخصصی دارید که نیاز به ارکستراسیون پویا دارند، هنگام ساخت سیستم‌های مکالمه‌ای که به قابلیت‌های مختلف هدایت می‌شوند، یا وقتی می‌خواهید منعطف‌ترین و سازگارترین رفتار عامل را داشته باشید.

## تبریک!

شما دورهٔ LangChain4j برای مبتدیان را به پایان رساندید. شما آموختید:

- چگونه هوش مصنوعی مکالمه‌ای با حافظه بسازید (ماژول 01)
- الگوهای مهندسی پرامپت برای وظایف مختلف (ماژول 02)
- زمینه‌سازی پاسخ‌ها در اسناد با RAG (ماژول 03)
- ایجاد عامل‌های پایه‌ای (دستیارها) با ابزارهای سفارشی (ماژول 04)
- یکپارچه‌سازی ابزارهای استانداردشده با LangChain4j MCP و Agentic (ماژول 05)

### مرحله بعدی چیست؟

پس از تکمیل ماژول‌ها، [راهنمای تست](../docs/TESTING.md) را بررسی کنید تا مفاهیم تست LangChain4j را در عمل ببینید.

**منابع رسمی:**
- [مستندات LangChain4j](https://docs.langchain4j.dev/) - راهنماهای جامع و مرجع API
- [گیت‌هاب LangChain4j](https://github.com/langchain4j/langchain4j) - کد منبع و مثال‌ها
- [آموزش‌های LangChain4j](https://docs.langchain4j.dev/tutorials/) - آموزش‌های گام‌به‌گام برای موارد استفادهٔ مختلف

از شما برای تکمیل این دوره متشکریم!

---

**ناوبری:** [← قبلی: ماژول 04 - ابزارها](../04-tools/README.md) | [بازگشت به صفحه اصلی](../README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
سلب مسئولیت:
این سند با استفاده از سرویس ترجمهٔ مبتنی بر هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما برای دقت تلاش می‌کنیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است حاوی خطاها یا نادرستی‌هایی باشند. نسخهٔ اصلی سند به زبان مبدأ باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، ترجمهٔ حرفه‌ای انسانی توصیه می‌شود. ما در قبال هرگونه سوءتفاهم یا برداشت نادرستی که از استفاده از این ترجمه ناشی شود، مسئولیتی نداریم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->