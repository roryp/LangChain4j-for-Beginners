<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "377b3e3e6f8d02965bf0fbbc9ccb45c5",
  "translation_date": "2025-12-13T14:33:02+00:00",
  "source_file": "00-quick-start/README.md",
  "language_code": "fa"
}
-->
# ماژول ۰۰: شروع سریع

## فهرست مطالب

- [مقدمه](../../../00-quick-start)
- [LangChain4j چیست؟](../../../00-quick-start)
- [وابستگی‌های LangChain4j](../../../00-quick-start)
- [پیش‌نیازها](../../../00-quick-start)
- [راه‌اندازی](../../../00-quick-start)
  - [۱. دریافت توکن گیت‌هاب](../../../00-quick-start)
  - [۲. تنظیم توکن](../../../00-quick-start)
- [اجرای مثال‌ها](../../../00-quick-start)
  - [۱. چت پایه](../../../00-quick-start)
  - [۲. الگوهای پرامپت](../../../00-quick-start)
  - [۳. فراخوانی تابع](../../../00-quick-start)
  - [۴. پرسش و پاسخ سند (RAG)](../../../00-quick-start)
- [هر مثال چه چیزی نشان می‌دهد](../../../00-quick-start)
- [گام‌های بعدی](../../../00-quick-start)
- [عیب‌یابی](../../../00-quick-start)

## مقدمه

این شروع سریع برای این است که شما را هرچه سریع‌تر با LangChain4j راه‌اندازی کند. این آموزش اصول پایه ساخت برنامه‌های هوش مصنوعی با LangChain4j و مدل‌های گیت‌هاب را پوشش می‌دهد. در ماژول‌های بعدی از Azure OpenAI با LangChain4j برای ساخت برنامه‌های پیشرفته‌تر استفاده خواهید کرد.

## LangChain4j چیست؟

LangChain4j یک کتابخانه جاوا است که ساخت برنامه‌های هوش مصنوعی را ساده می‌کند. به جای کار با کلاینت‌های HTTP و تجزیه JSON، با APIهای تمیز جاوا کار می‌کنید.

"زنجیره" در LangChain به اتصال چندین مؤلفه به هم اشاره دارد - ممکن است یک پرامپت را به یک مدل و سپس به یک پارسر متصل کنید، یا چندین فراخوانی هوش مصنوعی را به هم زنجیره کنید که خروجی یکی ورودی بعدی باشد. این شروع سریع بر اصول پایه تمرکز دارد قبل از اینکه زنجیره‌های پیچیده‌تر را بررسی کنیم.

<img src="../../../translated_images/langchain-concept.ad1fe6cf063515e1.fa.png" alt="LangChain4j Chaining Concept" width="800"/>

*اتصال مؤلفه‌ها در LangChain4j - بلوک‌های سازنده به هم متصل می‌شوند تا گردش‌های کاری قدرتمند هوش مصنوعی بسازند*

ما از سه مؤلفه اصلی استفاده خواهیم کرد:

**ChatLanguageModel** - رابط تعامل با مدل هوش مصنوعی. با `model.chat("prompt")` فراخوانی می‌کنید و یک رشته پاسخ دریافت می‌کنید. ما از `OpenAiOfficialChatModel` استفاده می‌کنیم که با نقاط انتهایی سازگار با OpenAI مانند مدل‌های گیت‌هاب کار می‌کند.

**AiServices** - رابط‌های سرویس هوش مصنوعی نوع ایمن ایجاد می‌کند. متدها را تعریف کنید، با `@Tool` آنها را نشانه‌گذاری کنید و LangChain4j هماهنگی را مدیریت می‌کند. هوش مصنوعی به طور خودکار در صورت نیاز متدهای جاوای شما را فراخوانی می‌کند.

**MessageWindowChatMemory** - تاریخچه گفتگو را حفظ می‌کند. بدون این، هر درخواست مستقل است. با آن، هوش مصنوعی پیام‌های قبلی را به یاد می‌آورد و زمینه را در چندین نوبت حفظ می‌کند.

<img src="../../../translated_images/architecture.eedc993a1c576839.fa.png" alt="LangChain4j Architecture" width="800"/>

*معماری LangChain4j - مؤلفه‌های اصلی که با هم کار می‌کنند تا برنامه‌های هوش مصنوعی شما را قدرت دهند*

## وابستگی‌های LangChain4j

این شروع سریع از دو وابستگی Maven در [`pom.xml`](../../../00-quick-start/pom.xml) استفاده می‌کند:

```xml
<!-- Core LangChain4j library -->
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j</artifactId> <!-- Inherited from BOM in root pom.xml -->
</dependency>

<!-- OpenAI integration (works with GitHub Models) -->
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-open-ai-official</artifactId> <!-- Inherited from BOM in root pom.xml -->
</dependency>
```

ماژول `langchain4j-open-ai-official` کلاس `OpenAiOfficialChatModel` را فراهم می‌کند که به APIهای سازگار با OpenAI متصل می‌شود. مدل‌های گیت‌هاب از همان فرمت API استفاده می‌کنند، بنابراین نیازی به آداپتور خاصی نیست - فقط آدرس پایه را به `https://models.github.ai/inference` اشاره دهید.

## پیش‌نیازها

**استفاده از Dev Container؟** جاوا و Maven از قبل نصب شده‌اند. فقط به یک توکن دسترسی شخصی گیت‌هاب نیاز دارید.

**توسعه محلی:**
- جاوا ۲۱ به بالا، Maven 3.9 به بالا
- توکن دسترسی شخصی گیت‌هاب (دستورالعمل‌ها در ادامه)

> **توجه:** این ماژول از `gpt-4.1-nano` از مدل‌های گیت‌هاب استفاده می‌کند. نام مدل را در کد تغییر ندهید - برای کار با مدل‌های موجود گیت‌هاب پیکربندی شده است.

## راه‌اندازی

### ۱. دریافت توکن گیت‌هاب

۱. به [تنظیمات گیت‌هاب → توکن‌های دسترسی شخصی](https://github.com/settings/personal-access-tokens) بروید  
۲. روی "Generate new token" کلیک کنید  
۳. یک نام توصیفی بگذارید (مثلاً "LangChain4j Demo")  
۴. تاریخ انقضا تنظیم کنید (۷ روز توصیه می‌شود)  
۵. در بخش "Account permissions"، گزینه "Models" را روی "Read-only" بگذارید  
۶. روی "Generate token" کلیک کنید  
۷. توکن را کپی و ذخیره کنید - دیگر قابل مشاهده نیست

### ۲. تنظیم توکن

**گزینه ۱: استفاده از VS Code (توصیه شده)**

اگر از VS Code استفاده می‌کنید، توکن خود را در فایل `.env` در ریشه پروژه اضافه کنید:

اگر فایل `.env` وجود ندارد، فایل `.env.example` را به `.env` کپی کنید یا یک فایل `.env` جدید در ریشه پروژه بسازید.

**مثال فایل `.env`:**  
```bash
# در /workspaces/LangChain4j-for-Beginners/.env
GITHUB_TOKEN=your_token_here
```
  
سپس می‌توانید به سادگی روی هر فایل دمو (مثلاً `BasicChatDemo.java`) در اکسپلورر راست‌کلیک کرده و **"Run Java"** را انتخاب کنید یا از پیکربندی‌های اجرا در پنل Run and Debug استفاده کنید.

**گزینه ۲: استفاده از ترمینال**

توکن را به عنوان متغیر محیطی تنظیم کنید:

**Bash:**  
```bash
export GITHUB_TOKEN=your_token_here
```
  
**PowerShell:**  
```powershell
$env:GITHUB_TOKEN=your_token_here
```
  
## اجرای مثال‌ها

**استفاده از VS Code:** به سادگی روی هر فایل دمو در اکسپلورر راست‌کلیک کرده و **"Run Java"** را انتخاب کنید، یا از پیکربندی‌های اجرا در پنل Run and Debug استفاده کنید (مطمئن شوید که ابتدا توکن را در فایل `.env` اضافه کرده‌اید).

**استفاده از Maven:** همچنین می‌توانید از خط فرمان اجرا کنید:

### ۱. چت پایه

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```
  
### ۲. الگوهای پرامپت

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.PromptEngineeringDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.PromptEngineeringDemo
```
  
الگوهای zero-shot، few-shot، chain-of-thought و نقش‌محور را نشان می‌دهد.

### ۳. فراخوانی تابع

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ToolIntegrationDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ToolIntegrationDemo
```
  
هوش مصنوعی به طور خودکار متدهای جاوای شما را در صورت نیاز فراخوانی می‌کند.

### ۴. پرسش و پاسخ سند (RAG)

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.SimpleReaderDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.SimpleReaderDemo
```
  
سؤالاتی درباره محتوای `document.txt` بپرسید.

## هر مثال چه چیزی نشان می‌دهد

**چت پایه** - [BasicChatDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/BasicChatDemo.java)

از اینجا شروع کنید تا LangChain4j را در ساده‌ترین حالت ببینید. یک `OpenAiOfficialChatModel` ایجاد می‌کنید، با `.chat()` یک پرامپت ارسال می‌کنید و پاسخ دریافت می‌کنید. این پایه را نشان می‌دهد: چگونه مدل‌ها را با نقاط انتهایی و کلیدهای API سفارشی مقداردهی اولیه کنید. وقتی این الگو را فهمیدید، بقیه چیزها بر اساس آن ساخته می‌شوند.

```java
ChatLanguageModel model = OpenAiOfficialChatModel.builder()
    .baseUrl("https://models.github.ai/inference")
    .apiKey(System.getenv("GITHUB_TOKEN"))
    .modelName("gpt-4.1-nano")
    .build();

String response = model.chat("What is LangChain4j?");
System.out.println(response);
```
  
> **🤖 با [GitHub Copilot](https://github.com/features/copilot) Chat امتحان کنید:** فایل [`BasicChatDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/BasicChatDemo.java) را باز کنید و بپرسید:  
> - "چطور در این کد از مدل‌های گیت‌هاب به Azure OpenAI سوئیچ کنم؟"  
> - "چه پارامترهای دیگری را می‌توانم در OpenAiOfficialChatModel.builder() تنظیم کنم؟"  
> - "چطور پاسخ‌های استریمینگ اضافه کنم به جای اینکه منتظر پاسخ کامل بمانم؟"

**مهندسی پرامپت** - [PromptEngineeringDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/PromptEngineeringDemo.java)

حالا که می‌دانید چطور با مدل صحبت کنید، بیایید ببینیم چه چیزی به آن می‌گویید. این دمو از همان تنظیم مدل استفاده می‌کند اما چهار الگوی پرامپت مختلف را نشان می‌دهد. پرامپت‌های zero-shot برای دستورالعمل‌های مستقیم، few-shot که از مثال‌ها یاد می‌گیرند، chain-of-thought که مراحل استدلال را نشان می‌دهد، و پرامپت‌های نقش‌محور که زمینه را تنظیم می‌کنند. خواهید دید که همان مدل چگونه بر اساس نحوه بیان درخواست، نتایج بسیار متفاوتی می‌دهد.

```java
PromptTemplate template = PromptTemplate.from(
    "What's the best time to visit {{destination}} for {{activity}}?"
);

Prompt prompt = template.apply(Map.of(
    "destination", "Paris",
    "activity", "sightseeing"
));

String response = model.chat(prompt.text());
```
  
> **🤖 با [GitHub Copilot](https://github.com/features/copilot) Chat امتحان کنید:** فایل [`PromptEngineeringDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/PromptEngineeringDemo.java) را باز کنید و بپرسید:  
> - "تفاوت بین zero-shot و few-shot prompting چیست و کی باید از هر کدام استفاده کنم؟"  
> - "پارامتر دما چگونه پاسخ‌های مدل را تحت تأثیر قرار می‌دهد؟"  
> - "چه تکنیک‌هایی برای جلوگیری از حملات تزریق پرامپت در تولید وجود دارد؟"  
> - "چطور می‌توانم اشیاء PromptTemplate قابل استفاده مجدد برای الگوهای رایج بسازم؟"

**ادغام ابزار** - [ToolIntegrationDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ToolIntegrationDemo.java)

اینجاست که LangChain4j قدرتمند می‌شود. شما از `AiServices` برای ساخت یک دستیار هوش مصنوعی استفاده می‌کنید که می‌تواند متدهای جاوای شما را فراخوانی کند. فقط متدها را با `@Tool("توضیح")` نشانه‌گذاری کنید و LangChain4j بقیه کار را انجام می‌دهد - هوش مصنوعی به طور خودکار تصمیم می‌گیرد چه زمانی از هر ابزار استفاده کند بر اساس درخواست کاربر. این فراخوانی تابع را نشان می‌دهد، یک تکنیک کلیدی برای ساخت هوش مصنوعی که می‌تواند عمل کند، نه فقط پاسخ دهد.

```java
@Tool("Performs addition of two numeric values")
public double add(double a, double b) {
    return a + b;
}

MathAssistant assistant = AiServices.create(MathAssistant.class, model);
String response = assistant.chat("What is 25 plus 17?");
```
  
> **🤖 با [GitHub Copilot](https://github.com/features/copilot) Chat امتحان کنید:** فایل [`ToolIntegrationDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ToolIntegrationDemo.java) را باز کنید و بپرسید:  
> - "چطور نشانه‌گذاری @Tool کار می‌کند و LangChain4j پشت صحنه با آن چه می‌کند؟"  
> - "آیا هوش مصنوعی می‌تواند چند ابزار را به ترتیب برای حل مسائل پیچیده فراخوانی کند؟"  
> - "اگر یک ابزار استثنا پرتاب کند چه اتفاقی می‌افتد - چطور باید خطاها را مدیریت کنم؟"  
> - "چطور یک API واقعی به جای این مثال ماشین حساب ادغام کنم؟"

**پرسش و پاسخ سند (RAG)** - [SimpleReaderDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/SimpleReaderDemo.java)

اینجا پایه RAG (تولید تقویت‌شده با بازیابی) را می‌بینید. به جای تکیه بر داده‌های آموزشی مدل، محتوایی از [`document.txt`](../../../00-quick-start/document.txt) بارگذاری می‌کنید و در پرامپت می‌گنجانید. هوش مصنوعی بر اساس سند شما پاسخ می‌دهد، نه دانش عمومی‌اش. این اولین گام به سوی ساخت سیستم‌هایی است که می‌توانند با داده‌های خود شما کار کنند.

```java
Document document = FileSystemDocumentLoader.loadDocument("document.txt");
String content = document.text();

String prompt = "Based on this document: " + content + 
                "\nQuestion: What is the main topic?";
String response = model.chat(prompt);
```
  
> **توجه:** این روش ساده کل سند را در پرامپت بارگذاری می‌کند. برای فایل‌های بزرگ‌تر از ۱۰ کیلوبایت، محدودیت‌های زمینه را رد می‌کنید. ماژول ۰۳ به بخش‌بندی و جستجوی برداری برای سیستم‌های RAG تولیدی می‌پردازد.

> **🤖 با [GitHub Copilot](https://github.com/features/copilot) Chat امتحان کنید:** فایل [`SimpleReaderDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/SimpleReaderDemo.java) را باز کنید و بپرسید:  
> - "چطور RAG از توهمات هوش مصنوعی جلوگیری می‌کند در مقایسه با استفاده از داده‌های آموزشی مدل؟"  
> - "تفاوت این روش ساده با استفاده از تعبیه‌های برداری برای بازیابی چیست؟"  
> - "چطور این را برای مدیریت چند سند یا پایگاه‌های دانش بزرگ‌تر مقیاس‌بندی کنم؟"  
> - "بهترین روش‌ها برای ساختاردهی پرامپت به گونه‌ای که هوش مصنوعی فقط از زمینه ارائه شده استفاده کند چیست؟"

## اشکال‌زدایی

مثال‌ها شامل `.logRequests(true)` و `.logResponses(true)` هستند تا تماس‌های API را در کنسول نشان دهند. این به عیب‌یابی خطاهای احراز هویت، محدودیت نرخ یا پاسخ‌های غیرمنتظره کمک می‌کند. در تولید این گزینه‌ها را حذف کنید تا نویز لاگ کاهش یابد.

## گام‌های بعدی

**ماژول بعدی:** [۰۱-مقدمه - شروع کار با LangChain4j و gpt-5 روی Azure](../01-introduction/README.md)

---

**ناوبری:** [← بازگشت به اصلی](../README.md) | [بعدی: ماژول ۰۱ - مقدمه →](../01-introduction/README.md)

---

## عیب‌یابی

### اولین ساخت Maven

**مشکل:** اجرای اولیه `mvn clean compile` یا `mvn package` زمان زیادی می‌برد (۱۰-۱۵ دقیقه)

**علت:** Maven باید تمام وابستگی‌های پروژه (Spring Boot، کتابخانه‌های LangChain4j، SDKهای Azure و غیره) را در اولین ساخت دانلود کند.

**راه‌حل:** این رفتار طبیعی است. ساخت‌های بعدی بسیار سریع‌تر خواهند بود چون وابستگی‌ها به صورت محلی کش شده‌اند. زمان دانلود به سرعت شبکه شما بستگی دارد.

### نحو دستور Maven در PowerShell

**مشکل:** دستورات Maven با خطای `Unknown lifecycle phase ".mainClass=..."` شکست می‌خورند

**علت:** PowerShell علامت `=` را به عنوان عملگر انتساب متغیر تفسیر می‌کند و نحو ویژگی Maven را خراب می‌کند

**راه‌حل:** از عملگر توقف تجزیه `--%` قبل از دستور Maven استفاده کنید:

**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```
  
**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```
  
عملگر `--%` به PowerShell می‌گوید که تمام آرگومان‌های باقی‌مانده را بدون تفسیر به Maven منتقل کند.

### نمایش ایموجی در PowerShell ویندوز

**مشکل:** پاسخ‌های هوش مصنوعی در PowerShell کاراکترهای نامفهوم (مثلاً `????` یا `â??`) به جای ایموجی نشان می‌دهند

**علت:** کدگذاری پیش‌فرض PowerShell از ایموجی‌های UTF-8 پشتیبانی نمی‌کند

**راه‌حل:** این دستور را قبل از اجرای برنامه‌های جاوا اجرا کنید:  
```cmd
chcp 65001
```
  
این کدگذاری UTF-8 را در ترمینال اجباری می‌کند. به طور جایگزین، از Windows Terminal استفاده کنید که پشتیبانی بهتری از یونیکد دارد.

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**سلب مسئولیت**:  
این سند با استفاده از سرویس ترجمه هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما در تلاش برای دقت هستیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است حاوی خطاها یا نادرستی‌هایی باشند. سند اصلی به زبان بومی خود باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، ترجمه حرفه‌ای انسانی توصیه می‌شود. ما مسئول هیچ گونه سوءتفاهم یا تفسیر نادرستی که از استفاده این ترجمه ناشی شود، نیستیم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->