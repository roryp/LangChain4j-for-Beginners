<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "22b5d7c8d7585325e38b37fd29eafe25",
  "translation_date": "2026-01-05T21:40:55+00:00",
  "source_file": "00-quick-start/README.md",
  "language_code": "fa"
}
-->
# ماژول ۰۰: شروع سریع

## فهرست مطالب

- [مقدمه](../../../00-quick-start)
- [LangChain4j چیست؟](../../../00-quick-start)
- [وابستگی‌های LangChain4j](../../../00-quick-start)
- [پیش‌نیازها](../../../00-quick-start)
- [راه‌اندازی](../../../00-quick-start)
  - [۱. گرفتن توکن گیت‌هاب](../../../00-quick-start)
  - [۲. تنظیم توکن خود](../../../00-quick-start)
- [اجرای مثال‌ها](../../../00-quick-start)
  - [۱. گفتگو پایه](../../../00-quick-start)
  - [۲. الگوهای پرامپت](../../../00-quick-start)
  - [۳. فراخوانی توابع](../../../00-quick-start)
  - [۴. پرسش و پاسخ سند (RAG)](../../../00-quick-start)
  - [۵. هوش مصنوعی مسئولانه](../../../00-quick-start)
- [هر مثال چه چیزی را نشان می‌دهد](../../../00-quick-start)
- [گام‌های بعدی](../../../00-quick-start)
- [عیب‌یابی](../../../00-quick-start)

## مقدمه

این شروع سریع برای این است که شما را هرچه سریع‌تر با LangChain4j راه‌اندازی کنیم. این راهنما اصول پایه ساخت برنامه‌های هوش مصنوعی با LangChain4j و مدل‌های گیت‌هاب را پوشش می‌دهد. در ماژول‌های بعدی از Azure OpenAI با LangChain4j برای ساخت برنامه‌های پیشرفته‌تر استفاده خواهید کرد.

## LangChain4j چیست؟

LangChain4j یک کتابخانه جاوا است که ساخت برنامه‌های هوش مصنوعی را ساده می‌کند. به جای کار با کلاینت‌های HTTP و تجزیه JSON، با APIهای پاک جاوا کار می‌کنید.

"زنجیره" در LangChain به متصل کردن چندین مؤلفه به هم اشاره دارد - ممکن است یک پرامپت به مدل و به یک پارسر متصل شود، یا چند فراخوانی هوش مصنوعی را به گونه‌ای زنجیروار به هم متصل کنید که خروجی یکی به ورودی بعدی برود. این شروع سریع روی مبانی تمرکز دارد قبل از اینکه به زنجیره‌های پیچیده‌تر بپردازد.

<img src="../../../translated_images/langchain-concept.ad1fe6cf063515e1.fa.png" alt="LangChain4j Chaining Concept" width="800"/>

*متصل کردن مؤلفه‌ها در LangChain4j - بلوک‌های ساختمانی که به هم وصل می‌شوند تا گردش‌کارهای قدرتمند هوش مصنوعی ایجاد کنند*

ما سه مؤلفه اصلی را استفاده خواهیم کرد:

**ChatLanguageModel** - رابط تعامل با مدل هوش مصنوعی. فراخوانی `model.chat("prompt")` و دریافت رشته پاسخ. ما از `OpenAiOfficialChatModel` استفاده می‌کنیم که با نقاط پایان سازگار با OpenAI مانند مدل‌های گیت‌هاب کار می‌کند.

**AiServices** - ایجاد رابط‌های سرویس هوش مصنوعی با نوع امن. متدها را تعریف کنید، آنها را با `@Tool` نشانه‌گذاری کنید و LangChain4j هماهنگی را مدیریت می‌کند. هوش مصنوعی به صورت خودکار هنگام نیاز متدهای جاوای شما را فراخوانی می‌کند.

**MessageWindowChatMemory** - حفظ تاریخچه مکالمه. بدون این، هر درخواست مستقل است. با آن، هوش مصنوعی پیام‌های قبلی را به یاد دارد و زمینه را در چندین نوبت حفظ می‌کند.

<img src="../../../translated_images/architecture.eedc993a1c576839.fa.png" alt="LangChain4j Architecture" width="800"/>

*معماری LangChain4j - مؤلفه‌های اصلی که با هم کار می‌کنند تا برنامه‌های هوش مصنوعی شما را توانمند سازند*

## وابستگی‌های LangChain4j

این شروع سریع از دو وابستگی Maven در [`pom.xml`](../../../00-quick-start/pom.xml) استفاده می‌کند:

```xml
<!-- Core LangChain4j library -->
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j</artifactId> <!-- Inherited from BOM in root pom.xml -->
</dependency>

<!-- OpenAI integration (works with GitHub Models) -->
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-open-ai-official</artifactId> <!-- Inherited from BOM in root pom.xml -->
</dependency>
```

ماژول `langchain4j-open-ai-official` کلاس `OpenAiOfficialChatModel` را فراهم می‌کند که به APIهای سازگار با OpenAI متصل می‌شود. مدل‌های گیت‌هاب از همان فرمت API استفاده می‌کنند، بنابراین نیازی به آداپتور خاصی نیست - فقط آدرس پایه را به `https://models.github.ai/inference` تنظیم کنید.

## پیش‌نیازها

**استفاده از Dev Container؟** جاوا و Maven از قبل نصب شده‌اند. فقط به توکن دسترسی شخصی گیت‌هاب نیاز دارید.

**توسعه محلی:**
- جاوا ۲۱ به بالا، Maven 3.9 به بالا
- توکن دسترسی شخصی گیت‌هاب (دستورالعمل‌ها در پایین)

> **توجه:** این ماژول از `gpt-4.1-nano` از مدل‌های گیت‌هاب استفاده می‌کند. نام مدل را در کد تغییر ندهید - این تنظیم شده است تا با مدل‌های موجود گیت‌هاب کار کند.

## راه‌اندازی

### ۱. گرفتن توکن گیت‌هاب

۱. به [تنظیمات گیت‌هاب → توکن‌های دسترسی شخصی](https://github.com/settings/personal-access-tokens) بروید  
۲. روی "Generate new token" کلیک کنید  
۳. یک نام توصیفی بگذارید (مثلاً "LangChain4j Demo")  
۴. تاریخ انقضا تنظیم کنید (۷ روز توصیه می‌شود)  
۵. در بخش "Account permissions" گزینه "Models" را روی "Read-only" تنظیم کنید  
۶. روی "Generate token" کلیک کنید  
۷. توکن را کپی و ذخیره کنید - دیگر آن را نخواهید دید

### ۲. تنظیم توکن خود

**گزینه ۱: استفاده از VS Code (توصیه شده)**

اگر از VS Code استفاده می‌کنید، توکن خود را به فایل `.env` در ریشه پروژه اضافه کنید:

اگر فایل `.env` وجود ندارد، فایل `.env.example` را کپی کنید به `.env` یا یک فایل جدید `.env` در ریشه پروژه بسازید.

**نمونه فایل `.env`:**  
```bash
# در /workspaces/LangChain4j-for-Beginners/.env
GITHUB_TOKEN=your_token_here
```
  
سپس می‌توانید روی هر فایل نمونه (مثلاً `BasicChatDemo.java`) در اکسپلورر راست‌کلیک کنید و گزینه **"Run Java"** را انتخاب کنید یا از تنظیمات لانچ در پنل Run and Debug استفاده نمایید.

**گزینه ۲: استفاده از ترمینال**

توکن را به عنوان متغیر محیطی تنظیم کنید:

**Bash:**  
```bash
export GITHUB_TOKEN=your_token_here
```
  
**PowerShell:**  
```powershell
$env:GITHUB_TOKEN=your_token_here
```
  
## اجرای مثال‌ها

**استفاده از VS Code:** به سادگی روی هر فایل نمونه در اکسپلورر راست‌کلیک و گزینه **"Run Java"** را انتخاب کنید، یا از تنظیمات لانچ در پنل Run and Debug استفاده کنید (مطمئن شوید که توکن را قبلاً به فایل `.env` اضافه کرده‌اید).

**استفاده از Maven:** یا می‌توانید از خط فرمان اجرا کنید:

### ۱. گفتگو پایه

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```
  
### ۲. الگوهای پرامپت

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.PromptEngineeringDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.PromptEngineeringDemo
```
  
الگوهای zero-shot، few-shot، chain-of-thought، و role-based را نشان می‌دهد.

### ۳. فراخوانی توابع

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ToolIntegrationDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ToolIntegrationDemo
```
  
هوش مصنوعی به صورت خودکار هنگام نیاز متدهای جاوای شما را فراخوانی می‌کند.

### ۴. پرسش و پاسخ سند (RAG)

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.SimpleReaderDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.SimpleReaderDemo
```
  
سؤالاتی درباره محتوای فایل `document.txt` بپرسید.

### ۵. هوش مصنوعی مسئولانه

**Bash:**  
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ResponsibleAIDemo
```
  
**PowerShell:**  
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ResponsibleAIDemo
```
  
مشاهده فیلترهای ایمنی هوش مصنوعی که محتوای مضر را مسدود می‌کنند.

## هر مثال چه چیزی را نشان می‌دهد

**گفتگو پایه** - [BasicChatDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/BasicChatDemo.java)

از اینجا شروع کنید تا LangChain4j را در ساده‌ترین حالت ببینید. یک `OpenAiOfficialChatModel` ایجاد می‌کنید، یک پرامپت با `.chat()` می‌فرستید و پاسخ دریافت می‌کنید. این اساس را نشان می‌دهد: چگونه مدل‌ها را با نقاط پایان و کلیدهای API سفارشی مقداردهی اولیه کنید. وقتی این الگو را فهمیدید، بقیه موارد روی آن ساخته می‌شود.

```java
ChatLanguageModel model = OpenAiOfficialChatModel.builder()
    .baseUrl("https://models.github.ai/inference")
    .apiKey(System.getenv("GITHUB_TOKEN"))
    .modelName("gpt-4.1-nano")
    .build();

String response = model.chat("What is LangChain4j?");
System.out.println(response);
```
  
> **🤖 امتحان با [GitHub Copilot](https://github.com/features/copilot) Chat:** فایل [`BasicChatDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/BasicChatDemo.java) را باز کنید و بپرسید:  
> - "چگونه در این کد از مدل‌های گیت‌هاب به Azure OpenAI تغییر دهم؟"  
> - "چه پارامترهای دیگری را می‌توانم در OpenAiOfficialChatModel.builder() تنظیم کنم؟"  
> - "چگونه می‌توانم پاسخ‌های استریمینگ اضافه کنم به جای انتظار برای دریافت پاسخ کامل؟"

**طراحی پرامپت** - [PromptEngineeringDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/PromptEngineeringDemo.java)

حالا که می‌دانید چگونه با مدل صحبت کنید، بیایید ببینیم چه چیزی به آن می‌گویید. این دمو از همان تنظیم مدل استفاده می‌کند اما چهار الگوی مختلف پرامپت را نشان می‌دهد. پرامپت‌های zero-shot برای دستورالعمل‌های مستقیم، few-shot که از مثال‌ها یاد می‌گیرند، chain-of-thought که مراحل استدلال را آشکار می‌کنند، و role-based که زمینه را تنظیم می‌کنند. خواهید دید که همان مدل چگونه نتایج مختلفی بر اساس نحوه چارچوب‌بندی درخواست ارائه می‌دهد.

```java
PromptTemplate template = PromptTemplate.from(
    "What's the best time to visit {{destination}} for {{activity}}?"
);

Prompt prompt = template.apply(Map.of(
    "destination", "Paris",
    "activity", "sightseeing"
));

String response = model.chat(prompt.text());
```
  
> **🤖 امتحان با [GitHub Copilot](https://github.com/features/copilot) Chat:** فایل [`PromptEngineeringDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/PromptEngineeringDemo.java) را باز کنید و بپرسید:  
> - "تفاوت بین پرامپت zero-shot و few-shot چیست و هر کدام چه زمانی استفاده می‌شوند؟"  
> - "پارامتر دما چگونه پاسخ‌های مدل را تحت تأثیر قرار می‌دهد؟"  
> - "چه تکنیک‌هایی برای جلوگیری از حملات تزریق پرامپت در تولید وجود دارد؟"  
> - "چگونه می‌توانم اشیای PromptTemplate قابل استفاده مجدد برای الگوهای رایج بسازم؟"

**ادغام ابزارها** - [ToolIntegrationDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ToolIntegrationDemo.java)

اینجا جایی است که LangChain4j قدرتمند می‌شود. با `AiServices` یک دستیار هوش مصنوعی ایجاد می‌کنید که می‌تواند متدهای جاوای شما را فراخوانی کند. فقط متدها را با `@Tool("توضیح")` نشانه‌گذاری کنید و LangChain4j بقیه را مدیریت می‌کند - هوش مصنوعی به صورت خودکار تصمیم می‌گیرد چه زمانی هر ابزار را بسته به درخواست کاربر استفاده کند. این فراخوانی تابع را نشان می‌دهد، یک تکنیک کلیدی برای ساخت هوش مصنوعی که نه فقط پاسخ می‌دهد بلکه اقدام می‌کند.

```java
@Tool("Performs addition of two numeric values")
public double add(double a, double b) {
    return a + b;
}

MathAssistant assistant = AiServices.create(MathAssistant.class, model);
String response = assistant.chat("What is 25 plus 17?");
```
  
> **🤖 امتحان با [GitHub Copilot](https://github.com/features/copilot) Chat:** فایل [`ToolIntegrationDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ToolIntegrationDemo.java) را باز کنید و بپرسید:  
> - "نشانه‌گذاری `@Tool` چگونه کار می‌کند و LangChain4j پشت صحنه چه می‌کند؟"  
> - "آیا هوش مصنوعی می‌تواند چند ابزار را به طور زنجیروار برای حل مسائل پیچیده فراخوانی کند؟"  
> - "اگر یک ابزار خطا پرتاب کند چه اتفاقی می‌افتد - چگونه باید خطاها را مدیریت کنم؟"  
> - "چگونه یک API واقعی را به جای این مثال ماشین‌حساب ادغام کنم؟"

**پرسش و پاسخ سند (RAG)** - [SimpleReaderDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/SimpleReaderDemo.java)

اینجا پایه‌های RAG (تولید تقویت‌شده بازیابی) را می‌بینید. به جای تکیه بر داده‌های آموزشی مدل، محتوا را از [`document.txt`](../../../00-quick-start/document.txt) بارگذاری می‌کنید و آن را در پرامپت وارد می‌کنید. هوش مصنوعی بر اساس سند شما پاسخ می‌دهد، نه دانش عمومی خود. این گام اول به سوی ساخت سیستم‌هایی است که با داده‌های خودتان کار می‌کنند.

```java
Document document = FileSystemDocumentLoader.loadDocument("document.txt");
String content = document.text();

String prompt = "Based on this document: " + content + 
                "\nQuestion: What is the main topic?";
String response = model.chat(prompt);
```
  
> **توجه:** این رویکرد ساده تمام سند را در پرامپت می‌ریزد. برای فایل‌های بزرگ‌تر از ۱۰کیلوبایت، محدودیت‌های زمینه را خواهید گذراند. ماژول ۰۳ به بخش‌بندی و جستجوی برداری برای سیستم‌های RAG تولیدی می‌پردازد.

> **🤖 امتحان با [GitHub Copilot](https://github.com/features/copilot) Chat:** فایل [`SimpleReaderDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/SimpleReaderDemo.java) را باز کنید و بپرسید:  
> - "چگونه RAG از هذیان‌های هوش مصنوعی نسبت به استفاده از داده‌های آموزشی مدل جلوگیری می‌کند؟"  
> - "تفاوت این رویکرد ساده با استفاده از درج برداری برای بازیابی چیست؟"  
> - "چگونه این را برای مدیریت چند سند یا پایگاه‌های دانش بزرگ‌تر مقیاس‌پذیر کنم؟"  
> - "بهترین روش‌ها برای ساختاردهی پرامپت تا مطمئن شویم هوش مصنوعی فقط از زمینه ارائه شده استفاده می‌کند چیست؟"

**هوش مصنوعی مسئولانه** - [ResponsibleAIDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ResponsibleAIDemo.java)

ایمنی هوش مصنوعی را با دفاع در عمق بسازید. این دمو دو لایه حفاظت را نشان می‌دهد که با هم کار می‌کنند:

**بخش ۱: LangChain4j Input Guardrails** - مسیر پرامپت‌های خطرناک را قبل از رسیدن به مدل زبانی بزرگ مسدود کنید. گاردریل‌های سفارشی بسازید که برای کلیدواژه‌ها یا الگوهای ممنوعه چک می‌کنند. این‌ها در کد شما اجرا می‌شوند؛ بنابراین سریع و رایگان هستند.

```java
class DangerousContentGuardrail implements InputGuardrail {
    @Override
    public InputGuardrailResult validate(UserMessage userMessage) {
        String text = userMessage.singleText().toLowerCase();
        if (text.contains("explosives")) {
            return fatal("Blocked: contains prohibited keyword");
        }
        return success();
    }
}
```
  
**بخش ۲: فیلترهای ایمنی فراهم‌کننده** - مدل‌های گیت‌هاب فیلترهای داخلی دارند که آنچه گاردریل‌های شما ممکن است از دست بدهند را می‌گیرند. شما مسدودهای سخت (خطاهای HTTP 400) برای نقض‌های جدی و ردهای نرم که هوش مصنوعی مودبانه رد می‌کند خواهید دید.

> **🤖 امتحان با [GitHub Copilot](https://github.com/features/copilot) Chat:** فایل [`ResponsibleAIDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ResponsibleAIDemo.java) را باز کنید و بپرسید:  
> - "InputGuardrail چیست و چگونه خودم بسازمش؟"  
> - "تفاوت مسدود سخت و رد نرم چیست؟"  
> - "چرا هم گاردریل و هم فیلترهای فراهم‌کننده را با هم استفاده کنیم؟"

## گام‌های بعدی

**ماژول بعدی:** [۰۱-مقدمه - شروع با LangChain4j و gpt-5 در Azure](../01-introduction/README.md)

---

**ناوبری:** [← بازگشت به اصلی](../README.md) | [بعدی: ماژول ۰۱ - مقدمه →](../01-introduction/README.md)

---

## عیب‌یابی

### اولین ساخت Maven

**مشکل:** اجرای اولیه `mvn clean compile` یا `mvn package` زمان‌بر است (۱۰-۱۵ دقیقه)

**دلیل:** Maven باید تمام وابستگی‌های پروژه (Spring Boot, کتابخانه‌های LangChain4j, SDKهای Azure و غیره) را در اولین ساخت دانلود کند.

**راه‌حل:** این رفتار عادی است. ساخت‌های بعدی بسیار سریع‌تر خواهند بود چون وابستگی‌ها به صورت محلی کش می‌شوند. زمان دانلود به سرعت شبکه شما بستگی دارد.

### نحو فرمان Maven در PowerShell

**مشکل:** فرمان‌های Maven با خطای `Unknown lifecycle phase ".mainClass=..."` شکست می‌خورند

**دلیل:** PowerShell علامت `=` را به عنوان عملگر انتساب متغیر تعبیر می‌کند و نحو ویژگی Maven را خراب می‌کند.
**راه‌حل**: از عملگر توقف پارسینگ `--%` قبل از دستور Maven استفاده کنید:

**PowerShell:**
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```

**Bash:**
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```

عملگر `--%` به PowerShell می‌گوید که تمام آرگومان‌های باقی‌مانده را بدون تفسیر به صورت دقیق به Maven منتقل کند.

### نمایش ایموجی در Windows PowerShell

**مشکل**: پاسخ‌های هوش مصنوعی به جای ایموجی‌ها کاراکترهای نامفهوم (مثلاً `????` یا `â??`) نشان می‌دهند در PowerShell

**علت**: کدگذاری پیش‌فرض PowerShell از ایموجی‌های UTF-8 پشتیبانی نمی‌کند

**راه‌حل**: این دستور را قبل از اجرای برنامه‌های جاوا اجرا کنید:
```cmd
chcp 65001
```

این دستور کدگذاری UTF-8 را در ترمینال فعال می‌کند. به‌طور جایگزین، می‌توانید از Windows Terminal استفاده کنید که پشتیبانی بهتری از یونیکد دارد.

### اشکال‌زدایی تماس‌های API

**مشکل**: خطاهای احراز هویت، محدودیت نرخ، یا پاسخ‌های غیرمنتظره از مدل هوش مصنوعی

**راه‌حل**: مثال‌ها شامل `.logRequests(true)` و `.logResponses(true)` هستند تا تماس‌های API در کنسول نمایش داده شود. این به شناسایی خطاهای احراز هویت، محدودیت‌های نرخ، یا پاسخ‌های غیرمنتظره کمک می‌کند. برای کاهش نویز لاگ در محیط تولید، این گزینه‌ها را حذف کنید.

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**سلب مسئولیت**:  
این سند با استفاده از سرویس ترجمه هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. اگرچه ما در تلاش برای دقت هستیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است شامل خطاها یا نادقتی‌هایی باشند. سند اصلی به زبان مبدأ باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، توصیه می‌شود از ترجمه حرفه‌ای انسانی استفاده شود. ما مسئول هیچگونه سوءتفاهم یا برداشت نادرستی که ناشی از استفاده از این ترجمه باشد، نیستیم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->