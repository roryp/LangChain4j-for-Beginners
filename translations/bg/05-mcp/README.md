<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "6c816d130a1fa47570c11907e72d84ae",
  "translation_date": "2026-01-06T01:11:12+00:00",
  "source_file": "05-mcp/README.md",
  "language_code": "bg"
}
-->
# Модул 05: Протокол за контекст на модела (MCP)

## Съдържание

- [Какво ще научите](../../../05-mcp)
- [Какво е MCP?](../../../05-mcp)
- [Как работи MCP](../../../05-mcp)
- [Агентен модул](../../../05-mcp)
- [Изпълнение на примерите](../../../05-mcp)
  - [Изисквания](../../../05-mcp)
- [Бърз старт](../../../05-mcp)
  - [Операции с файлове (Stdio)](../../../05-mcp)
  - [Супервизор агент](../../../05-mcp)
    - [Разбиране на изхода](../../../05-mcp)
    - [Стратегии за отговор](../../../05-mcp)
    - [Обяснение на функциите на агентния модул](../../../05-mcp)
- [Ключови концепции](../../../05-mcp)
- [Честито!](../../../05-mcp)
  - [Какво следва?](../../../05-mcp)

## Какво ще научите

Вече сте изградили разговорен AI, овладяли сте подсказките, свързали отговорите с документи и създали агенти с инструменти. Но всички тези инструменти бяха специално разработени за вашето конкретно приложение. А ако можехте да дадете на вашия AI достъп до стандартизирана екосистема от инструменти, които всеки може да създава и споделя? В този модул ще научите как да направите точно това с Протокола за контекст на модела (MCP) и агентния модул на LangChain4j. Първо показваме прост MCP четец на файлове и след това как лесно се интегрира в напреднали агентни работни потоци, използвайки модела на Супервизор агента.

## Какво е MCP?

Протоколът за контекст на модела (MCP) предоставя точно това - стандартизиран начин за AI приложенията да откриват и използват външни инструменти. Вместо да пишете потребителски интеграции за всеки източник на данни или услуга, вие се свързвате с MCP сървъри, които експонират техните възможности в последователен формат. Вашият AI агент след това може автоматично да открива и използва тези инструменти.

<img src="../../../translated_images/bg/mcp-comparison.9129a881ecf10ff5.png" alt="MCP Comparison" width="800"/>

*Преди MCP: Комплексни интеграции точка до точка. След MCP: Един протокол, безкрайни възможности.*

MCP решава основен проблем в разработката на AI: всяка интеграция е потребителска. Искате достъп до GitHub? Пишете потребителски код. Искате да четете файлове? Потребителски код. Искате да правите заявки към база данни? Потребителски код. И никоя от тези интеграции не работи с други AI приложения.

MCP стандартизира това. MCP сървър експонира инструменти с ясни описания и схеми. Всеки MCP клиент може да се свърже, да открие наличните инструменти и да ги използва. Създайте веднъж, използвайте навсякъде.

<img src="../../../translated_images/bg/mcp-architecture.b3156d787a4ceac9.png" alt="MCP Architecture" width="800"/>

*Архитектура на Протокола за контекст на модела - стандартизирано откриване и изпълнение на инструменти*

## Как работи MCP

**Архитектура клиент-сървър**

MCP използва модел клиент-сървър. Сървърите предоставят инструменти - четене на файлове, правене на заявки към бази данни, извикване на API-та. Клиентите (вашето AI приложение) се свързват със сървърите и използват техните инструменти.

За да използвате MCP с LangChain4j, добавете тази зависимост в Maven:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-mcp</artifactId>
    <version>${langchain4j.version}</version>
</dependency>
```

**Откриване на инструменти**

Когато клиентът ви се свърже с MCP сървър, той пита "Какви инструменти имате?" Сървърът отговаря със списък с налични инструменти, всеки с описания и схеми на параметрите. Вашият AI агент след това решава кои инструменти да използва според заявките на потребителя.

**Транспортни механизми**

MCP поддържа различни транспортни механизми. Този модул демонстрира транспорт Stdio за локални процеси:

<img src="../../../translated_images/bg/transport-mechanisms.2791ba7ee93cf020.png" alt="Transport Mechanisms" width="800"/>

*Транспортни механизми на MCP: HTTP за отдалечени сървъри, Stdio за локални процеси*

**Stdio** - [StdioTransportDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java)

За локални процеси. Вашето приложение стартира сървър като подпроцес и комуникира чрез стандартен вход/изход. Полезно за достъп до файлова система или командни инструменти.

```java
McpTransport stdioTransport = new StdioMcpTransport.Builder()
    .command(List.of(
        npmCmd, "exec",
        "@modelcontextprotocol/server-filesystem@2025.12.18",
        resourcesDir
    ))
    .logEvents(false)
    .build();
```

> **🤖 Опитайте с [GitHub Copilot](https://github.com/features/copilot) Chat:** Отворете [`StdioTransportDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java) и попитайте:
> - "Как работи транспортът Stdio и кога да го използвам вместо HTTP?"
> - "Как LangChain4j управлява жизнения цикъл на стартираните MCP сървърни процеси?"
> - "Какви са последиците за сигурността при даване достъп на AI до файлова система?"

## Агентен модул

Докато MCP предоставя стандартизирани инструменти, агентният модул на LangChain4j предлага декларативен начин за изграждане на агенти, които оркестрират тези инструменти. Анотацията `@Agent` и `AgenticServices` ви позволяват да дефинирате поведението на агента чрез интерфейси, а не чрез императивен код.

В този модул ще разгледате модела на **Супервизор агента** — напреднал подход за агентен AI, при който "супервизор" агент динамично решава кои подагенти да бъде извикани според заявките на потребителя. Ще комбинираме и двата концепта, като дадем на един от нашите подагенти възможности за достъп до файлове чрез MCP.

За да използвате агентния модул, добавете тази зависимост в Maven:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-agentic</artifactId>
    <version>${langchain4j.mcp.version}</version>
</dependency>
```

> **⚠️ Експериментално:** Модулът `langchain4j-agentic` е **експериментален** и може да бъде променян. Стабилният начин за изграждане на AI асистенти остава `langchain4j-core` с потребителски инструменти (Модул 04).

## Изпълнение на примерите

### Изисквания

- Java 21+, Maven 3.9+
- Node.js 16+ и npm (за MCP сървъри)
- Конфигурирани променливи на средата в `.env` файл (от кореновата директория):
  - `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_DEPLOYMENT` (същите като за Модул 01-04)

> **Забележка:** Ако още не сте настроили променливите на средата, вижте [Модул 00 - Бърз старт](../00-quick-start/README.md) за инструкции или копирайте `.env.example` в `.env` в кореновата директория и попълнете вашите стойности.

## Бърз старт

**Използване на VS Code:** Просто кликнете с десен бутон върху произволен демо файл в Explorer и изберете **"Run Java"**, или използвайте конфигурациите за стартиране от панела Run and Debug (преди това се уверете, че сте добавили токена си във файла `.env`).

**Използване на Maven:** Алтернативно можете да стартирате от командния ред с примерите по-долу.

### Операции с файлове (Stdio)

Това демонстрира локални инструменти, базирани на подпроцеси.

**✅ Няма нужда от предварителни изисквания** – MCP сървърът се стартира автоматично.

**Използване на стартовите скриптове (препоръчително):**

Стартовите скриптове автоматично зареждат променливите на средата от основния `.env` файл:

**Bash:**
```bash
cd 05-mcp
chmod +x start-stdio.sh
./start-stdio.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start-stdio.ps1
```

**Използване на VS Code:** Кликнете с десен бутон върху `StdioTransportDemo.java` и изберете **"Run Java"** (уверете се, че вашият `.env` файл е конфигуриран).

Приложението стартира MCP сървър за файлова система автоматично и чете локален файл. Обърнете внимание как управлението на подпроцесите се поема автоматично.

**Очакван изход:**
```
Assistant response: The file provides an overview of LangChain4j, an open-source Java library
for integrating Large Language Models (LLMs) into Java applications...
```

### Супервизор агент

Моделът на **Супервизор агента** е **гъвкав** вид агентен AI. Супервизорът използва LLM, за да реши автономно кои агенти да извика на база заявката на потребителя. В следващия пример комбинираме достъп до файлове чрез MCP с LLM агент, за да създадем контролирана последователност файл → отчет.

В демото, `FileAgent` чете файл с помощта на инструменти на MCP за файлова система, а `ReportAgent` генерира структуриран доклад с изпълнително резюме (1 изречение), 3 ключови точки и препоръки. Супервизорът оркестрира потока автоматично:

<img src="../../../translated_images/bg/agentic.cf84dcda226374e3.png" alt="Agentic Module" width="800"/>

```
┌─────────────┐      ┌──────────────┐
│  FileAgent  │ ───▶ │ ReportAgent  │
│ (MCP tools) │      │  (pure LLM)  │
└─────────────┘      └──────────────┘
   outputKey:           outputKey:
  'fileContent'         'report'
```

Всеки агент записва изхода си в **Agentic Scope** (споделена памет), което позволява на следващите агенти да достъпват предишните резултати. Това демонстрира как инструментите на MCP се интегрират безпроблемно в агентни работни потоци — Супервизорът не трябва да знае *как* се четат файловете, само че `FileAgent` може да го направи.

#### Изпълнение на демото

Стартовите скриптове автоматично зареждат променливите на средата от основния `.env` файл:

**Bash:**
```bash
cd 05-mcp
chmod +x start-supervisor.sh
./start-supervisor.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start-supervisor.ps1
```

**Използване на VS Code:** Кликнете с десен бутон върху `SupervisorAgentDemo.java` и изберете **"Run Java"** (уверете се, че вашият `.env` файл е конфигуриран).

#### Как работи Супервизорът

```java
// Стъпка 1: FileAgent чете файлове, използвайки MCP инструменти
FileAgent fileAgent = AgenticServices.agentBuilder(FileAgent.class)
        .chatModel(model)
        .toolProvider(mcpToolProvider)  // Притежава MCP инструменти за файлови операции
        .build();

// Стъпка 2: ReportAgent генерира структурирани отчети
ReportAgent reportAgent = AgenticServices.agentBuilder(ReportAgent.class)
        .chatModel(model)
        .build();

// Supervisor координира работния процес файл → отчет
SupervisorAgent supervisor = AgenticServices.supervisorBuilder()
        .chatModel(model)
        .subAgents(fileAgent, reportAgent)
        .responseStrategy(SupervisorResponseStrategy.LAST)  // Връща крайния отчет
        .build();

// Supervisor решава кои агенти да се използват въз основа на заявката
String response = supervisor.invoke("Read the file at /path/file.txt and generate a report");
```

#### Стратегии за отговор

Когато конфигурирате `SupervisorAgent`, определяте как той да формулира окончателния отговор към потребителя, след като подагентите завършат своите задачи. Наличните стратегии са:

| Стратегия | Описание |
|----------|-------------|
| **LAST** | Супервизорът връща изхода на последния извикан подагент или инструмент. Това е полезно, когато последният агент в работния поток е специално създаден да даде пълен, окончателен отговор (например "Агент за обобщение" в изследователски процес). |
| **SUMMARY** | Супервизорът използва собствен вътрешен езиков модел (LLM), за да синтезира резюме на цялата интеракция и всички изходи на подагентите, след което връща това резюме като окончателен отговор. Това предоставя ясен, агрегиращ отговор на потребителя. |
| **SCORED** | Системата използва вътрешен LLM, който оценява и последния отговор (LAST), и резюмето на интеракцията (SUMMARY) спрямо оригиналната потребителска заявка, връщайки изхода с по-висока оценка. |

Вижте [SupervisorAgentDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) за пълната реализация.

> **🤖 Опитайте с [GitHub Copilot](https://github.com/features/copilot) Chat:** Отворете [`SupervisorAgentDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) и попитайте:
> - "Как Супервизорът решава кои агенти да извика?"
> - "Каква е разликата между моделите Супервизор и Последователен работен поток?"
> - "Как мога да персонализирам поведението на планиране на Супервизора?"

#### Разбиране на изхода

Когато стартирате демото, ще видите структурирано обяснение за това как Супервизорът оркестрира множество агенти. Ето какво означава всяка секция:

```
======================================================================
  FILE → REPORT WORKFLOW DEMO
======================================================================

This demo shows a clear 2-step workflow: read a file, then generate a report.
The Supervisor orchestrates the agents automatically based on the request.
```

**Заглавката** въвежда концепцията за работния поток: фокусиран процес от четене на файл до генериране на доклад.

```
--- WORKFLOW ---------------------------------------------------------
  ┌─────────────┐      ┌──────────────┐
  │  FileAgent  │ ───▶ │ ReportAgent  │
  │ (MCP tools) │      │  (pure LLM)  │
  └─────────────┘      └──────────────┘
   outputKey:           outputKey:
   'fileContent'        'report'

--- AVAILABLE AGENTS -------------------------------------------------
  [FILE]   FileAgent   - Reads files via MCP → stores in 'fileContent'
  [REPORT] ReportAgent - Generates structured report → stores in 'report'
```

**Диаграмата на работния поток** показва потока на данните между агентите. Всеки агент има специфична роля:
- **FileAgent** чете файлове чрез MCP инструменти и записва суровото съдържание в `fileContent`
- **ReportAgent** използва това съдържание и създава структуриран доклад в `report`

```
--- USER REQUEST -----------------------------------------------------
  "Read the file at .../file.txt and generate a report on its contents"
```

**Потребителската заявка** показва задачата. Супервизорът я разчита и решава да извика FileAgent → ReportAgent.

```
--- SUPERVISOR ORCHESTRATION -----------------------------------------
  The Supervisor decides which agents to invoke and passes data between them...

  +-- STEP 1: Supervisor chose -> FileAgent (reading file via MCP)
  |
  |   Input: .../file.txt
  |
  |   Result: LangChain4j is an open-source, provider-agnostic Java framework for building LLM...
  +-- [OK] FileAgent (reading file via MCP) completed

  +-- STEP 2: Supervisor chose -> ReportAgent (generating structured report)
  |
  |   Input: LangChain4j is an open-source, provider-agnostic Java framew...
  |
  |   Result: Executive Summary...
  +-- [OK] ReportAgent (generating structured report) completed
```

**Оркестрация на Супервизора** показва двустепенния поток в действие:
1. **FileAgent** чете файла чрез MCP и записва съдържанието
2. **ReportAgent** получава съдържанието и генерира структуриран доклад

Супервизорът взема тези решения **автономно** според заявката на потребителя.

```
--- FINAL RESPONSE ---------------------------------------------------
Executive Summary
...

Key Points
...

Recommendations
...

--- AGENTIC SCOPE (Data Flow) ----------------------------------------
  Each agent stores its output for downstream agents to consume:
  * fileContent: LangChain4j is an open-source, provider-agnostic Java framework...
  * report: Executive Summary...
```

#### Обяснение на функциите на агентния модул

Примерът демонстрира няколко напреднали функционалности на агентния модул. Нека разгледаме Agentic Scope и Agent Listeners по-детайлно.

**Agentic Scope** показва споделената памет, където агентите записват резултатите си с `@Agent(outputKey="...")`. Това позволява:
- Следващи агенти да достъпват резултатите на предишни агенти
- Супервизорът да синтезира окончателен отговор
- Вас да инспектирате какво е произвел всеки агент

```java
ResultWithAgenticScope<String> result = supervisor.invokeWithAgenticScope(request);
AgenticScope scope = result.agenticScope();
String fileContent = scope.readState("fileContent");  // Сурови файлови данни от FileAgent
String report = scope.readState("report");            // Структуриран доклад от ReportAgent
```

**Agent Listeners** позволяват наблюдение и дебъгване на изпълнението на агенти. Стъпка по стъпка изходът, който виждате в демото, идва от AgentListener, който се свързва с всяко извикване на агент:
- **beforeAgentInvocation** - Извиква се когато супервизорът избира агент, позволявайки ви да видите кой агент е избран и защо
- **afterAgentInvocation** - Извиква се след приключване на агент, показвайки резултата му
- **inheritedBySubagents** - Когато е вярно, слушателят наблюдава всички агенти в иерархията

```java
AgentListener monitor = new AgentListener() {
    private int step = 0;
    
    @Override
    public void beforeAgentInvocation(AgentRequest request) {
        step++;
        System.out.println("  +-- STEP " + step + ": " + request.agentName());
    }
    
    @Override
    public void afterAgentInvocation(AgentResponse response) {
        System.out.println("  +-- [OK] " + response.agentName() + " completed");
    }
    
    @Override
    public boolean inheritedBySubagents() {
        return true; // Разпространете до всички подагенти
    }
};
```

Освен модела на Супервизор, модулът `langchain4j-agentic` предоставя няколко мощни патерна за работни потоци и функции:

| Патерн | Описание | Пример за използване |
|---------|-------------|----------|
| **Последователен** | Изпълнява агентите поред, изходът се подава на следващия | Поток: изследване → анализ → доклад |
| **Паралелен** | Изпълнява агентите едновременно | Независими задачи: прогноза за времето + новини + акции |
| **Цикличен** | Итерация докато се изпълни условие | Оценка на качество: повторение докато оценката ≥ 0.8 |
| **Условен** | Рутинг според условия | Класификация → рутинг към специалист |
| **Човек в цикъла** | Добавяне на човешки проверки | Работни потоци с одобрения, преглед на съдържание |

## Ключови концепции

След като разгледахте MCP и агентния модул в действие, нека обобщим кога да използвате всеки подход.

**MCP** е идеален, когато искате да използвате съществуващи екосистеми от инструменти, да създавате инструменти, които да споделят множество приложения, да интегрирате външни услуги с общи протоколи или да сменяте имплементации на инструменти без промени в кода.

**Агентният модул** работи най-добре когато искате декларативни дефиниции на агенти с анотации `@Agent`, нуждаете се от оркестрация на работни потоци (последователни, циклични, паралелни), предпочитате дизайн на агентите базиран на интерфейси вместо императивен код или комбинирате няколко агента, които споделят изход чрез `outputKey`.

**Моделът на Супервизор агента** се откроява, когато работният поток не е предсказуем предварително и искате LLM да решава, когато имате няколко специализирани агента, които изискват динамична оркестрация, при изграждане на разговорни системи с маршрутизация към различни способности или когато искате най-гъвкавото и адаптивно поведение на агента.
## Поздравления!

Вие завършихте курса LangChain4j за начинаещи. Вие научихте:

- Как да изграждате разговорен AI с памет (Модул 01)
- Шаблони за създаване на заявки за различни задачи (Модул 02)
- Основа на отговори в документи с RAG (Модул 03)
- Създаване на базови AI агенти (асистенти) с персонализирани инструменти (Модул 04)
- Интегриране на стандартизирани инструменти с LangChain4j MCP и Agentic модули (Модул 05)

### Какво следва?

След завършване на модулите, разгледайте [Testing Guide](../docs/TESTING.md), за да видите концепциите за тестване на LangChain4j в действие.

**Официални ресурси:**
- [LangChain4j Documentation](https://docs.langchain4j.dev/) - Изчерпателни ръководства и референтни API
- [LangChain4j GitHub](https://github.com/langchain4j/langchain4j) - Изходен код и примери
- [LangChain4j Tutorials](https://docs.langchain4j.dev/tutorials/) - Стъпка по стъпка уроци за различни случаи на употреба

Благодарим Ви, че завършихте този курс!

---

**Навигация:** [← Предишен: Модул 04 - Инструменти](../04-tools/README.md) | [Обратно към Началото](../README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от отговорност**:
Този документ е преведен с помощта на AI преводаческа услуга [Co-op Translator](https://github.com/Azure/co-op-translator). Въпреки че се стремим към точност, моля имайте предвид, че автоматизираните преводи могат да съдържат грешки или неточности. Оригиналният документ на неговия език трябва да се счита за авторитетен източник. За критична информация се препоръчва професионален човешки превод. Не носим отговорност за никакви недоразумения или неправилни тълкувания, произтичащи от използването на този превод.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->