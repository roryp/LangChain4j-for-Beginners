<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "6c816d130a1fa47570c11907e72d84ae",
  "translation_date": "2026-01-05T21:37:59+00:00",
  "source_file": "05-mcp/README.md",
  "language_code": "ar"
}
-->
# الوحدة 05: بروتوكول سياق النموذج (MCP)

## جدول المحتويات

- [ما الذي ستتعلمه](../../../05-mcp)
- [ما هو MCP؟](../../../05-mcp)
- [كيف يعمل MCP](../../../05-mcp)
- [الوحدة الوكيلة](../../../05-mcp)
- [تشغيل الأمثلة](../../../05-mcp)
  - [المتطلبات الأساسية](../../../05-mcp)
- [البدء السريع](../../../05-mcp)
  - [عمليات الملفات (Stdio)](../../../05-mcp)
  - [وكيل المشرف](../../../05-mcp)
    - [فهم المخرجات](../../../05-mcp)
    - [استراتيجيات الاستجابة](../../../05-mcp)
    - [شرح ميزات الوحدة الوكيلة](../../../05-mcp)
- [المفاهيم الأساسية](../../../05-mcp)
- [تهانينا!](../../../05-mcp)
  - [ما التالي؟](../../../05-mcp)

## ما الذي ستتعلمه

لقد أنشأت ذكاءً اصطناعيًا حواريًا، وأتقنت إنشاء المحفزات، وأرسيت الردود على الوثائق، وأنشأت وكلاء مزودين بالأدوات. لكن كل تلك الأدوات كانت مخصصة لتطبيقك الخاص. ماذا لو كان بإمكانك منح الذكاء الاصطناعي حق الوصول إلى نظام بيئي موحد من الأدوات يمكن لأي شخص إنشاؤه ومشاركته؟ في هذه الوحدة، ستتعلم كيفية القيام بذلك تمامًا باستخدام بروتوكول سياق النموذج (MCP) ووحدة LangChain4j الوكيلة. نبدأ بعرض قارئ ملفات MCP بسيط ثم نوضح كيف يندمج بسهولة مع سير عمل وكيل متقدم باستخدام نمط وكيل المشرف.

## ما هو MCP؟

يوفر بروتوكول سياق النموذج (MCP) هذا بالضبط - طريقة معيارية لتطبيقات الذكاء الاصطناعي لاكتشاف واستخدام الأدوات الخارجية. بدلاً من كتابة تكاملات مخصصة لكل مصدر بيانات أو خدمة، تتصل بخوادم MCP التي تعرض قدراتها بصيغة موحدة. يمكن لوكيل الذكاء الاصطناعي الخاص بك بعد ذلك اكتشاف هذه الأدوات واستخدامها تلقائيًا.

<img src="../../../translated_images/mcp-comparison.9129a881ecf10ff5.ar.png" alt="MCP Comparison" width="800"/>

*قبل MCP: تكاملات معقدة نقطة لنقطة. بعد MCP: بروتوكول واحد، إمكانيات لا نهائية.*

يحل MCP مشكلة أساسية في تطوير الذكاء الاصطناعي: كل تكامل مخصص. هل تريد الوصول إلى GitHub؟ كود مخصص. هل تريد قراءة ملفات؟ كود مخصص. هل تريد الاستعلام من قاعدة بيانات؟ كود مخصص. ولا تعمل أي من هذه التكاملات مع تطبيقات الذكاء الاصطناعي الأخرى.

يوحد MCP هذا الأمر. يعرض خادم MCP الأدوات مع أوصاف واضحة ومخططات. يمكن لأي عميل MCP الاتصال، واكتشاف الأدوات المتاحة، واستخدامها. ابني مرة واحدة، واستخدم في كل مكان.

<img src="../../../translated_images/mcp-architecture.b3156d787a4ceac9.ar.png" alt="MCP Architecture" width="800"/>

*بنية بروتوكول سياق النموذج - اكتشاف وتنفيذ الأدوات موحد*

## كيف يعمل MCP

**بنية العميل-الخادم**

يستخدم MCP نموذج عميل-خادم. يقدم الخادم أدوات - قراءة ملفات، استعلام قواعد بيانات، استدعاء واجهات برمجة التطبيقات. يتصل العميل (تطبيق الذكاء الاصطناعي الخاص بك) بالخوادم ويستخدم أدواتها.

لاستخدام MCP مع LangChain4j، أضف تبعية Maven هذه:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-mcp</artifactId>
    <version>${langchain4j.version}</version>
</dependency>
```

**اكتشاف الأدوات**

عندما يتصل عميلك بخادم MCP، يسأل "ما الأدوات التي تمتلكها؟" يرد الخادم بقائمة الأدوات المتاحة، كل منها مع أوصاف ومخططات المعلمات. يمكن لوكيل الذكاء الاصطناعي الخاص بك حينها أن يقرر الأدوات التي يجب استخدامها بناءً على طلبات المستخدم.

**آليات النقل**

يدعم MCP آليات نقل مختلفة. توضح هذه الوحدة نقل Stdio للعمليات المحلية:

<img src="../../../translated_images/transport-mechanisms.2791ba7ee93cf020.ar.png" alt="Transport Mechanisms" width="800"/>

*آليات نقل MCP: HTTP للخوادم عن بعد، Stdio للعمليات المحلية*

**Stdio** - [StdioTransportDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java)

لعمليات محلية. يقوم تطبيقك بتشغيل خادم كعملية فرعية ويتواصل عبر الإدخال/الإخراج القياسي. مفيد للوصول إلى نظام الملفات أو أدوات سطر الأوامر.

```java
McpTransport stdioTransport = new StdioMcpTransport.Builder()
    .command(List.of(
        npmCmd, "exec",
        "@modelcontextprotocol/server-filesystem@2025.12.18",
        resourcesDir
    ))
    .logEvents(false)
    .build();
```

> **🤖 جرب مع [GitHub Copilot](https://github.com/features/copilot) Chat:** افتح [`StdioTransportDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java) واسأل:
> - "كيف يعمل نقل Stdio ومتى يجب استخدامه مقابل HTTP؟"
> - "كيف تدير LangChain4j دورة حياة عمليات خادم MCP المُشغلة؟"
> - "ما هي تبعات الأمان لمنح الذكاء الاصطناعي حق الوصول إلى نظام الملفات؟"

## الوحدة الوكيلة

بينما يوفر MCP أدوات موحدة، توفر وحدة LangChain4j **الوكيلة** طريقة إعلانية لبناء وكلاء ينظمون تلك الأدوات. تسمح لك التعليمة `@Agent` و`AgenticServices` بتعريف سلوك الوكيل من خلال الواجهات بدلًا من الكود الإجرائي.

في هذه الوحدة، ستستكشف نمط **وكيل المشرف** — وهو نهج وكيل ذكاء اصطناعي متقدم حيث يقرر وكيل "المشرف" بشكل ديناميكي الوكلاء الفرعيين الذين يجب استدعاؤهم بناءً على طلبات المستخدم. سنجمع بين المفهومين من خلال منح واحد من وكلائنا الفرعيين قدرات الوصول إلى الملفات المدعومة بواسطة MCP.

لاستخدام الوحدة الوكيلة، أضف تبعية Maven هذه:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-agentic</artifactId>
    <version>${langchain4j.mcp.version}</version>
</dependency>
```

> **⚠️ تجريبي:** وحدة `langchain4j-agentic` **تجريبية** وقابلة للتغيير. الطريقة المستقرة لبناء مساعدي الذكاء الاصطناعي تبقى استخدام `langchain4j-core` مع الأدوات المخصصة (الوحدة 04).

## تشغيل الأمثلة

### المتطلبات الأساسية

- Java 21+، Maven 3.9+
- Node.js 16+ و npm (لخوادم MCP)
- متغيرات البيئة مهيئة في ملف `.env` (من الدليل الجذري):
  - `AZURE_OPENAI_ENDPOINT`، `AZURE_OPENAI_API_KEY`، `AZURE_OPENAI_DEPLOYMENT` (كما في الوحدات 01-04)

> **ملاحظة:** إذا لم تقم بإعداد متغيرات البيئة حتى الآن، راجع [الوحدة 00 - البدء السريع](../00-quick-start/README.md) للتعليمات، أو انسخ `.env.example` إلى `.env` في الدليل الجذري واملأ القيم الخاصة بك.

## البدء السريع

**باستخدام VS Code:** فقط انقر بزر الماوس الأيمن على أي ملف عرض في المستعرض واختر **"تشغيل Java"**، أو استخدم تكوينات الإطلاق من لوحة التشغيل والتصحيح (تأكد من إضافة التوكن إلى ملف `.env` أولاً).

**باستخدام Maven:** بدلاً من ذلك، يمكنك التشغيل من سطر الأوامر مع الأمثلة أدناه.

### عمليات الملفات (Stdio)

يُظهر هذا الأدوات المعتمدة على العمليات الفرعية المحلية.

**✅ لا حاجة لمتطلبات مسبقة** - يتم توليد خادم MCP تلقائيًا.

**باستخدام سكريبتات البدء (موصى به):**

تقوم سكريبتات البدء بتحميل متغيرات البيئة من ملف `.env` الجذري تلقائيًا:

**Bash:**
```bash
cd 05-mcp
chmod +x start-stdio.sh
./start-stdio.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start-stdio.ps1
```

**باستخدام VS Code:** انقر بزر الماوس الأيمن على `StdioTransportDemo.java` واختر **"تشغيل Java"** (تأكد من تكوين ملف `.env` الخاص بك).

يقوم التطبيق بتشغيل خادم MCP لنظام الملفات تلقائيًا ويقرأ ملفًا محليًا. لاحظ كيف تتم إدارة العمليات الفرعية نيابة عنك.

**المخرجات المتوقعة:**
```
Assistant response: The file provides an overview of LangChain4j, an open-source Java library
for integrating Large Language Models (LLMs) into Java applications...
```

### وكيل المشرف

نمط **وكيل المشرف** هو شكل **مرن** من وكيل الذكاء الاصطناعي الوكلي. يستخدم المشرف نموذج لغة كبير لاتخاذ قرار مستقل بشأن الوكلاء الفرعيين الذين يجب استدعاؤهم بناءً على طلب المستخدم. في المثال التالي، نجمع بين الوصول المدعوم بـ MCP للملفات مع وكيل LLM لإنشاء سير عمل قراءة ملف → تقرير بإشراف.

في العرض التوضيحي، يقرأ `FileAgent` ملفًا باستخدام أدوات نظام الملفات MCP، وينشئ `ReportAgent` تقريرًا منظمًا مع ملخص تنفيذي (جملة واحدة)، 3 نقاط رئيسية، وتوصيات. ينظم المشرف هذا التدفق تلقائيًا:

<img src="../../../translated_images/agentic.cf84dcda226374e3.ar.png" alt="Agentic Module" width="800"/>

```
┌─────────────┐      ┌──────────────┐
│  FileAgent  │ ───▶ │ ReportAgent  │
│ (MCP tools) │      │  (pure LLM)  │
└─────────────┘      └──────────────┘
   outputKey:           outputKey:
  'fileContent'         'report'
```

يخزن كل وكيل مخرجاته في **نطاق الوكيل** (ذاكرة مشتركة)، مما يسمح للوكلاء التاليين بالوصول إلى النتائج السابقة. يظهر هذا كيف تندمج أدوات MCP بسلاسة في سير عمل الوكيل — لا يحتاج المشرف إلى معرفة *كيف* تُقرأ الملفات، بل فقط أن `FileAgent` يمكنه فعل ذلك.

#### تشغيل العرض التوضيحي

تقوم سكريبتات البدء بتحميل متغيرات البيئة من ملف `.env` الجذري تلقائيًا:

**Bash:**
```bash
cd 05-mcp
chmod +x start-supervisor.sh
./start-supervisor.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start-supervisor.ps1
```

**باستخدام VS Code:** انقر بزر الماوس الأيمن على `SupervisorAgentDemo.java` واختر **"تشغيل Java"** (تأكد من تكوين ملف `.env` الخاص بك).

#### كيف يعمل المشرف

```java
// الخطوة 1: يقرأ FileAgent الملفات باستخدام أدوات MCP
FileAgent fileAgent = AgenticServices.agentBuilder(FileAgent.class)
        .chatModel(model)
        .toolProvider(mcpToolProvider)  // يحتوي على أدوات MCP لعمليات الملفات
        .build();

// الخطوة 2: يقوم ReportAgent بإنشاء تقارير منظمة
ReportAgent reportAgent = AgenticServices.agentBuilder(ReportAgent.class)
        .chatModel(model)
        .build();

// المشرف ينظم تدفق العمل من الملفات إلى التقارير
SupervisorAgent supervisor = AgenticServices.supervisorBuilder()
        .chatModel(model)
        .subAgents(fileAgent, reportAgent)
        .responseStrategy(SupervisorResponseStrategy.LAST)  // إرجاع التقرير النهائي
        .build();

// يقرر المشرف أي الوكلاء يستدعي بناءً على الطلب
String response = supervisor.invoke("Read the file at /path/file.txt and generate a report");
```

#### استراتيجيات الاستجابة

عند تكوين `SupervisorAgent`، تحدد كيف يجب أن يصيغ إجابته النهائية للمستخدم بعد اكتمال وكلاء المهام الفرعية. الاستراتيجيات المتوفرة هي:

| الاستراتيجية | الوصف |
|--------------|-------|
| **الأخير** | يُعيد المشرف مخرجات آخر وكيل فرعي أو أداة تم استدعاؤها. هذا مفيد عندما يكون الوكيل النهائي في سير العمل مصممًا خصيصًا لإنتاج الإجابة النهائية الكاملة (مثل "وكيل الملخص" في سلسلة أبحاث). |
| **الملخص** | يستخدم المشرف نموذج اللغة الداخلي synthesize ملخصًا لكامل التفاعل وجميع مخرجات الوكلاء الفرعيين، ثم يُرجع هذا الملخص كرد نهائي. يوفر هذا جوابًا مجمعًا ونظيفًا للمستخدم. |
| **التقييم** | تستخدم النظام نموذج لغة داخلي لتقييم كل من الاستجابة الأخيرة وملخص التفاعل مقابل طلب المستخدم الأصلي، ثم يُرجع المخرجات التي حصلت على درجة أعلى. |

انظر [SupervisorAgentDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) للتنفيذ الكامل.

> **🤖 جرب مع [GitHub Copilot](https://github.com/features/copilot) Chat:** افتح [`SupervisorAgentDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) واسأل:
> - "كيف يقرر المشرف الوكلاء الذين يجب استدعاؤهم؟"
> - "ما الفرق بين أنماط المشرف وسير العمل المتسلسل؟"
> - "كيف يمكنني تخصيص سلوك تخطيط المشرف؟"

#### فهم المخرجات

عند تشغيل العرض، سترى شرحًا منظمًا لكيفية تنظيم المشرف لعدة وكلاء. إليك ما تعنيه كل قسم:

```
======================================================================
  FILE → REPORT WORKFLOW DEMO
======================================================================

This demo shows a clear 2-step workflow: read a file, then generate a report.
The Supervisor orchestrates the agents automatically based on the request.
```

**العنوان** يقدم مفهوم سير العمل: خط أنابيب مركّز من قراءة الملفات إلى إنشاء التقرير.

```
--- WORKFLOW ---------------------------------------------------------
  ┌─────────────┐      ┌──────────────┐
  │  FileAgent  │ ───▶ │ ReportAgent  │
  │ (MCP tools) │      │  (pure LLM)  │
  └─────────────┘      └──────────────┘
   outputKey:           outputKey:
   'fileContent'        'report'

--- AVAILABLE AGENTS -------------------------------------------------
  [FILE]   FileAgent   - Reads files via MCP → stores in 'fileContent'
  [REPORT] ReportAgent - Generates structured report → stores in 'report'
```

**مخطط سير العمل** يوضح تدفق البيانات بين الوكلاء. لكل وكيل دور محدد:
- **FileAgent** يقرأ الملفات باستخدام أدوات MCP ويخزن المحتوى الخام في `fileContent`
- **ReportAgent** يستهلك هذا المحتوى وينتج تقريرًا منظمًا في `report`

```
--- USER REQUEST -----------------------------------------------------
  "Read the file at .../file.txt and generate a report on its contents"
```

**طلب المستخدم** يعرض المهمة. يقوم المشرف بتحليلها ويقرر استدعاء FileAgent → ReportAgent.

```
--- SUPERVISOR ORCHESTRATION -----------------------------------------
  The Supervisor decides which agents to invoke and passes data between them...

  +-- STEP 1: Supervisor chose -> FileAgent (reading file via MCP)
  |
  |   Input: .../file.txt
  |
  |   Result: LangChain4j is an open-source, provider-agnostic Java framework for building LLM...
  +-- [OK] FileAgent (reading file via MCP) completed

  +-- STEP 2: Supervisor chose -> ReportAgent (generating structured report)
  |
  |   Input: LangChain4j is an open-source, provider-agnostic Java framew...
  |
  |   Result: Executive Summary...
  +-- [OK] ReportAgent (generating structured report) completed
```

**تنسيق المشرف** يظهر تدفق الخطوتين بالعمل:
1. **FileAgent** يقرأ الملف عبر MCP ويخزن المحتوى
2. **ReportAgent** يستلم المحتوى وينشئ تقريرًا منظمًا

اتخذ المشرف هذه القرارات **بشكل مستقل** بناءً على طلب المستخدم.

```
--- FINAL RESPONSE ---------------------------------------------------
Executive Summary
...

Key Points
...

Recommendations
...

--- AGENTIC SCOPE (Data Flow) ----------------------------------------
  Each agent stores its output for downstream agents to consume:
  * fileContent: LangChain4j is an open-source, provider-agnostic Java framework...
  * report: Executive Summary...
```

#### شرح ميزات الوحدة الوكيلة

يُظهر المثال عدة ميزات متقدمة للوحدة الوكيلة. لنلقِ نظرة أقرب على نطاق الوكيل ومستمعي الوكيل.

**نطاق الوكيل** يعرض الذاكرة المشتركة حيث يخزن الوكلاء نتائجهم باستخدام `@Agent(outputKey="...")`. هذا يسمح بـ:
- للوكلاء اللاحقين الوصول إلى مخرجات الوكلاء السابقين
- للمشرف بتوليف رد نهائي
- لك بفحص ما أنتجه كل وكيل

```java
ResultWithAgenticScope<String> result = supervisor.invokeWithAgenticScope(request);
AgenticScope scope = result.agenticScope();
String fileContent = scope.readState("fileContent");  // بيانات الملف الخام من FileAgent
String report = scope.readState("report");            // تقرير منظم من ReportAgent
```

**مستمعو الوكيل** يمكنون من مراقبة وتصحيح تنفيذ الوكلاء. الإخراج خطوة بخطوة في العرض يأتي من AgentListener يتصل بكل استدعاء وكيل:
- **beforeAgentInvocation** - يُستدعى عندما يختار المشرف وكيلًا، مما يتيح لك رؤية الوكيل المختار والسبب
- **afterAgentInvocation** - يُستدعى عندما يكمل الوكيل مهمته، مظهراً النتيجة
- **inheritedBySubagents** - عندما يكون صحيحًا، يراقب المستمع جميع الوكلاء في التسلسل الهرمي

```java
AgentListener monitor = new AgentListener() {
    private int step = 0;
    
    @Override
    public void beforeAgentInvocation(AgentRequest request) {
        step++;
        System.out.println("  +-- STEP " + step + ": " + request.agentName());
    }
    
    @Override
    public void afterAgentInvocation(AgentResponse response) {
        System.out.println("  +-- [OK] " + response.agentName() + " completed");
    }
    
    @Override
    public boolean inheritedBySubagents() {
        return true; // انشر إلى جميع الوكلاء الفرعيين
    }
};
```

بعيدًا عن نمط المشرف، توفر وحدة `langchain4j-agentic` عدة أنماط ميزات وسير عمل قوية:

| النمط | الوصف | حالة الاستخدام |
|-------|-------|----------------|
| **المتسلسل** | تنفيذ الوكلاء بترتيب، تنتقل المخرجات إلى التالي | خطوط الأنابيب: بحث → تحليل → تقرير |
| **الموازي** | تشغيل الوكلاء في نفس الوقت | مهام مستقلة: الطقس + الأخبار + الأسهم |
| **الحلقة** | التكرار حتى تحقيق شرط | تقييم الجودة: تحسين حتى الدرجة ≥ 0.8 |
| **شرطي** | التوجيه بناءً على الشروط | التصنيف → التوجيه إلى وكيل متخصص |
| **البشر في الحلقة** | إضافة نقاط تفتيش بشرية | سير عمل الموافقة، مراجعة المحتوى |

## المفاهيم الأساسية

الآن بعد أن استكشفت MCP والوحدة الوكيلة في العمل، دعنا نلخص متى تستخدم كل منهما.

**MCP** مثالي عندما تريد استغلال أنظمة أدوات موجودة، بناء أدوات يمكن لعدة تطبيقات مشاركتها، دمج خدمات طرف ثالث ببروتوكولات معيارية، أو تبديل تنفيذات الأدوات دون تغيير الكود.

**الوحدة الوكيلة** تعمل بشكل أفضل عندما تريد تعريف وكلاء إعلانيين باستخدام تعليقات `@Agent`، تحتاج إلى تنظيم سير عمل (متسلسل، حلقة، موازي)، تفضل تصميم الوكيل معتمدًا على الواجهات بدلاً من الكود الإجرائي، أو تجمع عدة وكلاء يشاركون المخرجات عبر `outputKey`.

**نمط وكيل المشرف** يبرز عندما لا يكون سير العمل متوقعًا مسبقًا وتريد أن يقرر نموذج اللغة الكبير، عندما تملك وكلاء متخصصين متعددين يحتاجون إلى تنسيق ديناميكي، عند بناء أنظمة حوارية توجه إلى قدرات مختلفة، أو عندما تريد أكثر سلوك وكيل مرن وتكيفًا.
## تهانينا!

لقد أكملت دورة LangChain4j للمبتدئين. لقد تعلمت:

- كيفية بناء ذكاء اصطناعي حواري مع ذاكرة (الوحدة 01)
- أنماط هندسة المطالبات لمهام مختلفة (الوحدة 02)
- ترسيخ الردود في وثائقك باستخدام RAG (الوحدة 03)
- إنشاء وكلاء ذكاء اصطناعي أساسيين (مساعدين) بأدوات مخصصة (الوحدة 04)
- دمج الأدوات المعيارية مع LangChain4j MCP ووحدات Agentic (الوحدة 05)

### ما الذي يلي؟

بعد إكمال الوحدات، استكشف [دليل الاختبار](../docs/TESTING.md) لرؤية مفاهيم اختبار LangChain4j قيد التنفيذ.

**الموارد الرسمية:**
- [وثائق LangChain4j](https://docs.langchain4j.dev/) - أدلة شاملة ومرجع API
- [LangChain4j على GitHub](https://github.com/langchain4j/langchain4j) - الشيفرة المصدرية والأمثلة
- [برامج تعليمية لـ LangChain4j](https://docs.langchain4j.dev/tutorials/) - دروس خطوة بخطوة لحالات استخدام متنوعة

شكرًا لك على إتمام هذه الدورة!

---

**التنقل:** [← السابق: الوحدة 04 - الأدوات](../04-tools/README.md) | [العودة إلى الرئيسي](../README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**إخلاء المسؤولية**:
تمت ترجمة هذا المستند باستخدام خدمة الترجمة الآلية [Co-op Translator](https://github.com/Azure/co-op-translator). بينما نسعى لتحقيق الدقة، يُرجى العلم أن الترجمات الآلية قد تحتوي على أخطاء أو عدم دقة. يجب اعتبار المستند الأصلي بلغته الأصلية المصدر الموثوق به. بالنسبة للمعلومات الهامة، يُنصح بالاستعانة بترجمة احترافية من قبل بشر. نحن غير مسؤولين عن أي سوء فهم أو تفسير ناتج عن استخدام هذه الترجمة.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->