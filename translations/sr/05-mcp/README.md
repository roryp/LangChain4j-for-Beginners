<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "6c816d130a1fa47570c11907e72d84ae",
  "translation_date": "2026-01-06T01:17:48+00:00",
  "source_file": "05-mcp/README.md",
  "language_code": "sr"
}
-->
# Модул 05: Протокол модела контекста (MCP)

## Садржај

- [Шта ћете научити](../../../05-mcp)
- [Шта је MCP?](../../../05-mcp)
- [Како MCP ради](../../../05-mcp)
- [Агентски модул](../../../05-mcp)
- [Покретање примера](../../../05-mcp)
  - [Претпоставке](../../../05-mcp)
- [Брзи почетак](../../../05-mcp)
  - [Операције са фајловима (Stdio)](../../../05-mcp)
  - [Супервизор агент](../../../05-mcp)
    - [Разумевање резултата](../../../05-mcp)
    - [Стратегије одговора](../../../05-mcp)
    - [Објашњење функција агентског модула](../../../05-mcp)
- [Кључни појмови](../../../05-mcp)
- [Честитамо!](../../../05-mcp)
  - [Шта следи?](../../../05-mcp)

## Шта ћете научити

Поставили сте конверзацијски AI, савладали упите, ослонили одговоре на документе и креирали агенте са алатима. Али сви ти алати били су прилагођени за вашу специфичну апликацију. Шта ако бисте могли да дате вашем AI приступ стандардизованом екосистему алата које свако може да креира и дели? У овом модулу ћете научити како то учинити помоћу Протокола модела контекста (MCP) и агентског модула LangChain4j. Прво приказујемо једноставан MCP читач фајлова, а затим показујемо како се лако интегрише у напредне агентске токове рада користећи образац Супервизор агента.

## Шта је MCP?

Протокол модела контекста (MCP) пружа управо то - стандардни начин да AI апликације откривају и користе спољашње алате. Уместо да пишете прилагођене интеграције за сваки извор података или услугу, повезујете се на MCP сервере који своје могућности приказују у конзистентном формату. Ваш AI агент онда аутоматски може да открије и користи те алате.

<img src="../../../translated_images/sr/mcp-comparison.9129a881ecf10ff5.png" alt="MCP Comparison" width="800"/>

*Пре MCP: Комплексне интеграције тачка-по-тачка. Након MCP: Један протокол, бескрајне могућности.*

MCP решава основни проблем у развоју AI: свака интеграција је прилагођена. Желите приступ GitHub-у? Прилагођени код. Желите да читате фајлове? Прилагођени код. Желите да упитате базу података? Прилагођени код. И ниједна од тих интеграција не ради са другим AI апликацијама.

MCP то стандардизује. MCP сервер приказује алате са јасним описима и шемама параметара. Сваки MCP клијент може да се повеже, открије доступне алате и користи их. Креирај једном, користи свуда.

<img src="../../../translated_images/sr/mcp-architecture.b3156d787a4ceac9.png" alt="MCP Architecture" width="800"/>

*Архитектура Протокола модела контекста - стандардизовано откривање и извршавање алата*

## Како MCP ради

**Архитектура сервер-клијент**

MCP користи модел клијент-сервер. Сервери пружају алате - читање фајлова, упити база података, позиви API-ја. Клијенти (ваша AI апликација) се повезују на сервере и користе њихове алате.

За коришћење MCP са LangChain4j додајте ову Maven зависност:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-mcp</artifactId>
    <version>${langchain4j.version}</version>
</dependency>
```

**Откривање алата**

Када се ваш клијент повеже на MCP сервер, пита „Које алате имаш?“ Сервер враћа листу доступних алата, сваки са описом и шемама параметара. Ваш AI агент онда може да одлучи које алате да користи на основу захтева корисника.

**Механизми транспорта**

MCP подржава различите механизме транспорта. Овај модул демонстрира Stdio транспорт за локалне процесе:

<img src="../../../translated_images/sr/transport-mechanisms.2791ba7ee93cf020.png" alt="Transport Mechanisms" width="800"/>

*MCP механизми транспорта: HTTP за удаљене сервере, Stdio за локалне процесе*

**Stdio** - [StdioTransportDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java)

За локалне процесе. Ваша апликација покреће сервер као подпроцес и комуницира преко стандардног улаза/излаза. Корисно за приступ датотечном систему или командно-линеарним алатима.

```java
McpTransport stdioTransport = new StdioMcpTransport.Builder()
    .command(List.of(
        npmCmd, "exec",
        "@modelcontextprotocol/server-filesystem@2025.12.18",
        resourcesDir
    ))
    .logEvents(false)
    .build();
```

> **🤖 Испробајте са [GitHub Copilot](https://github.com/features/copilot) Четом:** Отворите [`StdioTransportDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java) и питајте:
> - "Како Stdio транспорт функционише и када треба да га користим уместо HTTP?"
> - "Како LangChain4j управља животним циклусом покренутих MCP серверских процеса?"
> - "Који су безбедносни аспекти давања AI приступа датотечном систему?"

## Агентски модул

Док MCP пружа стандардизоване алате, LangChain4j-ов **агентски модул** даје декларативни начин за изградњу агената који оркестрирају те алате. Анотација `@Agent` и `AgenticServices` омогућавају да дефинишете понашање агента кроз интерфејсе уместо императивног кода.

У овом модулу истражујете образац **Супервизор агента** — напредни агентски приступ у којем супервизор динамички одлучује које под-агенте да позове на основу корисничких захтева. Комбинујемо оба концепта тако што једном од наших под-агената дајемо могућности приступа датотечном систему преко MCP алата.

За коришћење агентског модула додајте ову Maven зависност:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-agentic</artifactId>
    <version>${langchain4j.mcp.version}</version>
</dependency>
```

> **⚠️ Експериментално:** Модул `langchain4j-agentic` је **експерименталан** и може се мењати. Стабилни начин за израду AI асистената је и даље `langchain4j-core` са прилагођеним алатима (Модул 04).

## Покретање примера

### Претпоставке

- Java 21+, Maven 3.9+
- Node.js 16+ и npm (за MCP сервере)
- Конфигурисане системске променљиве у `.env` фајлу (из коренског директоријума):
  - `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_DEPLOYMENT` (исто као у Модулима 01-04)

> **Напомена:** Ако још нисте подесили системске променљиве, погледајте [Модул 00 - Брзи почетак](../00-quick-start/README.md) за упутства или копирајте `.env.example` у `.env` у коренском директоријуму и попуните своје вредности.

## Брзи почетак

**Коришћење VS Code:** Једноставно кликните десним на било који демо фајл у Explorer-у и изаберите **"Run Java"**, или користите конфигурације за покретање из Run and Debug панела (пре тога се уверите да сте додали свој токен у `.env` фајл).

**Коришћење Mavena:** Такође, можете покренути из командне линије са примерима испод.

### Операције са фајловима (Stdio)

Ово демонстрира алате засноване на локалним подпроцесима.

**✅ Нема претпоставки** - MCP сервер се аутоматски покреће.

**Коришћење старт скрипти (препоручено):**

Старт скрипте аутоматски учитавају системске променљиве из коренског `.env` фајла:

**Bash:**
```bash
cd 05-mcp
chmod +x start-stdio.sh
./start-stdio.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start-stdio.ps1
```

**Коришћење VS Code:** Кликните десним на `StdioTransportDemo.java` и изаберите **"Run Java"** (уверите се да је ваш `.env` фајл конфигурисан).

Апликација аутоматски покреће MCP сервер за датотечни систем и чита локални фајл. Обратите пажњу како је управљање подпроцесима решено за вас.

**Очекујени излаз:**
```
Assistant response: The file provides an overview of LangChain4j, an open-source Java library
for integrating Large Language Models (LLMs) into Java applications...
```

### Супервизор агент

Образац **Супервизор агента** је **флексибилан** облик агентског AI. Супервизор користи LLM да аутономно одлучи које агенте да позове на основу захтева корисника. У следећем примеру комбинујемо MCP-ом ојачани приступ фајловима са LLM агентом да креирамо надзорни ток рада читања фајла → извештај.

У демо приказу, `FileAgent` чита фајл користећи MCP алате датотечног система, а `ReportAgent` генерише структурирани извештај са извршним резимеом (1 реченица), 3 кључне тачке и препорукама. Супервизор аутоматски оркестрира овај ток:

<img src="../../../translated_images/sr/agentic.cf84dcda226374e3.png" alt="Agentic Module" width="800"/>

```
┌─────────────┐      ┌──────────────┐
│  FileAgent  │ ───▶ │ ReportAgent  │
│ (MCP tools) │      │  (pure LLM)  │
└─────────────┘      └──────────────┘
   outputKey:           outputKey:
  'fileContent'         'report'
```

Свак агент чува свој резултат у **Агентском обиму** (заједничка меморија), омогућавајући агентима у низу приступ претходним резултатима. Ово показује како се MCP алати беспрекорно интегришу у агентске токове – Супервизор не мора да зна *како* се фајлови читају, већ само да `FileAgent` то може.

#### Покретање демоа

Старт скрипте аутоматски учитавају системске променљиве из коренског `.env` фајла:

**Bash:**
```bash
cd 05-mcp
chmod +x start-supervisor.sh
./start-supervisor.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start-supervisor.ps1
```

**Коришћење VS Code:** Кликните десним на `SupervisorAgentDemo.java` и изаберите **"Run Java"** (уверите се да је ваш `.env` фајл конфигурисан).

#### Како Супервизор ради

```java
// Фаза 1: FileAgent чита датотеке користећи MCP алате
FileAgent fileAgent = AgenticServices.agentBuilder(FileAgent.class)
        .chatModel(model)
        .toolProvider(mcpToolProvider)  // Поседује MCP алате за операције са датотекама
        .build();

// Фаза 2: ReportAgent генерише структуиране извештаје
ReportAgent reportAgent = AgenticServices.agentBuilder(ReportAgent.class)
        .chatModel(model)
        .build();

// Supervisor координира радни ток од датотеке до извештаја
SupervisorAgent supervisor = AgenticServices.supervisorBuilder()
        .chatModel(model)
        .subAgents(fileAgent, reportAgent)
        .responseStrategy(SupervisorResponseStrategy.LAST)  // Враћа коначни извештај
        .build();

// Supervisor одлучује које агенте ће позвати у зависности од захтева
String response = supervisor.invoke("Read the file at /path/file.txt and generate a report");
```

#### Стратегије одговора

Када конфигуришете `SupervisorAgent`, одређујете како треба да формулише свој коначни одговор кориснику након што су под-агенти завршили своје задатке. Доступне стратегије су:

| Стратегија | Опис |
|------------|-------|
| **LAST** | Супервизор враћа излаз последњег позваног под-агента или алата. Ово је корисно када је последњи агент у току рада посебно дизајниран за производњу комплетног, коначног одговора (нпр. "Агент за резиме" у истраживачком току). |
| **SUMMARY** | Супервизор користи свој интерни језички модел (LLM) да синтетише резиме целе интеракције и свих резултата под-агената, а затим враћа тај резиме као коначни одговор. Пружа чист, агрегирани одговор кориснику. |
| **SCORED** | Систем користи интерни LLM да оцени и LAST одговор и SUMMARY интеракције у односу на оригинални кориснички захтев, враћајући онај излаз који добије већу оцену. |

Погледајте [SupervisorAgentDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) за пуну имплементацију.

> **🤖 Испробајте са [GitHub Copilot](https://github.com/features/copilot) Четом:** Отворите [`SupervisorAgentDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) и питајте:
> - "Како Супервизор одлучује које агенте да позове?"
> - "Која је разлика између образаца Супервизора и Секвенцијалног тока?"
> - "Како могу да прилагодим понашање планирања Супервизора?"

#### Разумевање резултата

Када покренете демо, видећете структуриран приказ како Супервизор оркестрира више агената. Ево шта сваки одељак значи:

```
======================================================================
  FILE → REPORT WORKFLOW DEMO
======================================================================

This demo shows a clear 2-step workflow: read a file, then generate a report.
The Supervisor orchestrates the agents automatically based on the request.
```

**Наслов** уводи концепт тока рада: фокусирана линија од читања фајла до генерисања извештаја.

```
--- WORKFLOW ---------------------------------------------------------
  ┌─────────────┐      ┌──────────────┐
  │  FileAgent  │ ───▶ │ ReportAgent  │
  │ (MCP tools) │      │  (pure LLM)  │
  └─────────────┘      └──────────────┘
   outputKey:           outputKey:
   'fileContent'        'report'

--- AVAILABLE AGENTS -------------------------------------------------
  [FILE]   FileAgent   - Reads files via MCP → stores in 'fileContent'
  [REPORT] ReportAgent - Generates structured report → stores in 'report'
```

**Дијаграм тока рада** приказује проток података између агената. Сваки агент има специфичну улогу:
- **FileAgent** чита фајлове користећи MCP алате и чува неуређени садржај у `fileContent`
- **ReportAgent** користи тај садржај и производи структурирани извештај у `report`

```
--- USER REQUEST -----------------------------------------------------
  "Read the file at .../file.txt and generate a report on its contents"
```

**Кориснички захтев** приказује задатак. Супервизор га анализира и одлучује да позове FileAgent → ReportAgent.

```
--- SUPERVISOR ORCHESTRATION -----------------------------------------
  The Supervisor decides which agents to invoke and passes data between them...

  +-- STEP 1: Supervisor chose -> FileAgent (reading file via MCP)
  |
  |   Input: .../file.txt
  |
  |   Result: LangChain4j is an open-source, provider-agnostic Java framework for building LLM...
  +-- [OK] FileAgent (reading file via MCP) completed

  +-- STEP 2: Supervisor chose -> ReportAgent (generating structured report)
  |
  |   Input: LangChain4j is an open-source, provider-agnostic Java framew...
  |
  |   Result: Executive Summary...
  +-- [OK] ReportAgent (generating structured report) completed
```

**Оркестрација Супервизора** приказује двостепени ток у акцији:
1. **FileAgent** чита фајл преко MCP и чува садржај
2. **ReportAgent** прима садржај и генерише структурирани извештај

Супервизор је ове одлуке донео **аутономно** на основу захтева корисника.

```
--- FINAL RESPONSE ---------------------------------------------------
Executive Summary
...

Key Points
...

Recommendations
...

--- AGENTIC SCOPE (Data Flow) ----------------------------------------
  Each agent stores its output for downstream agents to consume:
  * fileContent: LangChain4j is an open-source, provider-agnostic Java framework...
  * report: Executive Summary...
```

#### Објашњење функција агентског модула

Пример показује неколико напредних функција агентског модула. Погледајмо ближе Агентски обим и Осматраче агената.

**Агентски обим** приказује заједничку меморију у коју агенти чувају резултате користећи `@Agent(outputKey="...")`. Ово омогућава:
- Каснијим агентима да приступе излазима ранијих агената
- Супервизору да синтетише коначни одговор
- Вама да проверите шта је који агент произвео

```java
ResultWithAgenticScope<String> result = supervisor.invokeWithAgenticScope(request);
AgenticScope scope = result.agenticScope();
String fileContent = scope.readState("fileContent");  // Необрађени подаци из датотеке од FileAgent
String report = scope.readState("report");            // Структурирани извештај од ReportAgent
```

**Осматрачи агената** омогућавају праћење и дебаговање извршавања агената. Степеним корацима који се виде у демо пријављује AgentListener који се повезује на сваку позивницу агента:
- **beforeAgentInvocation** – Позива се када Супервизор бира агента, омогућава вам да видите који је агент изабран и зашто
- **afterAgentInvocation** – Позива се када агент заврши, приказује његов резултат
- **inheritedBySubagents** – Када је true, слушач прати све агенте у хијерархији

```java
AgentListener monitor = new AgentListener() {
    private int step = 0;
    
    @Override
    public void beforeAgentInvocation(AgentRequest request) {
        step++;
        System.out.println("  +-- STEP " + step + ": " + request.agentName());
    }
    
    @Override
    public void afterAgentInvocation(AgentResponse response) {
        System.out.println("  +-- [OK] " + response.agentName() + " completed");
    }
    
    @Override
    public boolean inheritedBySubagents() {
        return true; // Пропагирати на све под-агенте
    }
};
```

Осврћући се ван обрасца Супервизора, модул `langchain4j-agentic` пружа неколико моћних образаца токова рада и функција:

| Образац | Опис | Примена |
|---------|-------|---------|
| **Секвенцијални** | Извршава агенте један по један, излаз тече ка следећем | Токови рада: истраживање → анализа → извештај |
| **Паралелни** | Покреће агенте истовремено | Независни задаци: време + вести + акције |
| **Лoop** | Итерација док услов није испуњен | Оцена квалитета: усавршавај док ≥ 0.8 |
| **Условни** | Рутинг на основу услова | Класификација → рутирање ка специјалистичком агенту |
| **Људски у петљи** | Додаје људске контролне тачке | Радни токови одобрења, преглед садржаја |

## Кључни појмови

Сада када сте истражили MCP и агентски модул у пракси, хајде да резимирамо када користити који приступ.

**MCP** је идеалан када желите да искористите постојеће екосистеме алата, градите алате које различите апликације могу делити, интегришете сервисе трећих страна са стандардним протоколима, или мењате имплементације алата без промене кода.

**Агентски модул** најбоље функционише када желите декларативне дефиниције агената помоћу `@Agent` анотација, потребна вам је оркестрација токова рада (секвенцијално, петља, паралелно), више волите дизајн агената на бази интерфејса него императивни код, или комбинуте више агената који деле излазе путем `outputKey`.

**Образац Супервизор агента** је најбољи када ток рада није предвидив унапред и желите да LLM одлучује, када имате више специјализованих агената који захтевају динамичку оркестрацију, када правите конверзацијске системе који усмеравају ка различитим способностима, или када желите најфлексибилније и најадекватније понашање агента.
## Честитамо!

Завршили сте курс LangChain4j за почетнике. Научили сте:

- Како изградити разговорну вештачку интелигенцију са меморијом (Модул 01)
- Обрасце инжењеринга помоћних упита за различите задатке (Модул 02)
- Како засновати одговоре на вашим документима помоћу RAG (Модул 03)
- Креирање основних AI агената (асистената) са прилагођеним алатима (Модул 04)
- Интеграцију стандардизованих алата са LangChain4j MCP и Agentic модулима (Модул 05)

### Шта следи?

Након завршетка модула, погледајте [Водич за тестирање](../docs/TESTING.md) да бисте видели концепте тестирања LangChain4j у пракси.

**Званични ресурси:**
- [LangChain4j документација](https://docs.langchain4j.dev/) - Свеобухватни приручници и API референца
- [LangChain4j GitHub](https://github.com/langchain4j/langchain4j) - Изворни код и примери
- [LangChain4j туторијали](https://docs.langchain4j.dev/tutorials/) - Туторијали корак по корак за разне примене

Хвала вам што сте завршили овај курс!

---

**Навигација:** [← Претходно: Модул 04 - Алати](../04-tools/README.md) | [Назад на главну страну](../README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Одрицање од одговорности**:
Овај документ је преведен уз помоћ AI преводилачке услуге [Co-op Translator](https://github.com/Azure/co-op-translator). Иако настојимо да обезбедимо тачност, имајте у виду да аутоматски преводи могу садржати грешке или нетачности. Оригинални документ на његовом изворном језику треба сматрати ауторитетним извором. За критичне информације препоручује се стручно људско превођење. Нисмо одговорни за било каква неспоразума или погрешна тумачења која могу настати коришћењем овог превода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->