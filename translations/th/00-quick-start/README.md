<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "377b3e3e6f8d02965bf0fbbc9ccb45c5",
  "translation_date": "2025-12-13T14:57:29+00:00",
  "source_file": "00-quick-start/README.md",
  "language_code": "th"
}
-->
# Module 00: เริ่มต้นอย่างรวดเร็ว

## สารบัญ

- [บทนำ](../../../00-quick-start)
- [LangChain4j คืออะไร?](../../../00-quick-start)
- [การพึ่งพา LangChain4j](../../../00-quick-start)
- [ข้อกำหนดเบื้องต้น](../../../00-quick-start)
- [การตั้งค่า](../../../00-quick-start)
  - [1. รับโทเค็น GitHub ของคุณ](../../../00-quick-start)
  - [2. ตั้งค่าโทเค็นของคุณ](../../../00-quick-start)
- [รันตัวอย่าง](../../../00-quick-start)
  - [1. แชทพื้นฐาน](../../../00-quick-start)
  - [2. รูปแบบคำสั่ง](../../../00-quick-start)
  - [3. การเรียกฟังก์ชัน](../../../00-quick-start)
  - [4. ถามตอบเอกสาร (RAG)](../../../00-quick-start)
- [สิ่งที่แต่ละตัวอย่างแสดง](../../../00-quick-start)
- [ขั้นตอนถัดไป](../../../00-quick-start)
- [การแก้ไขปัญหา](../../../00-quick-start)

## บทนำ

การเริ่มต้นอย่างรวดเร็วนี้มีจุดประสงค์เพื่อให้คุณเริ่มใช้งาน LangChain4j ได้อย่างรวดเร็วที่สุด ครอบคลุมพื้นฐานสุดของการสร้างแอปพลิเคชัน AI ด้วย LangChain4j และ GitHub Models ในโมดูลถัดไปคุณจะใช้ Azure OpenAI กับ LangChain4j เพื่อสร้างแอปพลิเคชันที่ซับซ้อนมากขึ้น

## LangChain4j คืออะไร?

LangChain4j คือไลบรารี Java ที่ช่วยให้ง่ายต่อการสร้างแอปพลิเคชันที่ขับเคลื่อนด้วย AI แทนที่จะต้องจัดการกับ HTTP clients และการแยกวิเคราะห์ JSON คุณจะทำงานกับ API Java ที่สะอาด

คำว่า "chain" ใน LangChain หมายถึงการเชื่อมต่อส่วนประกอบหลายๆ ตัวเข้าด้วยกัน — คุณอาจเชื่อมต่อ prompt กับโมเดลกับ parser หรือเชื่อมต่อการเรียก AI หลายครั้งที่ผลลัพธ์หนึ่งป้อนเป็นอินพุตของถัดไป การเริ่มต้นอย่างรวดเร็วนี้เน้นที่พื้นฐานก่อนที่จะสำรวจ chain ที่ซับซ้อนมากขึ้น

<img src="../../../translated_images/langchain-concept.ad1fe6cf063515e1.th.png" alt="แนวคิดการเชื่อมต่อ LangChain4j" width="800"/>

*การเชื่อมต่อส่วนประกอบใน LangChain4j - บล็อกก่อสร้างเชื่อมต่อกันเพื่อสร้างเวิร์กโฟลว์ AI ที่ทรงพลัง*

เราจะใช้สามส่วนประกอบหลัก:

**ChatLanguageModel** - อินเทอร์เฟซสำหรับการโต้ตอบกับโมเดล AI เรียก `model.chat("prompt")` และรับสตริงตอบกลับ เราใช้ `OpenAiOfficialChatModel` ซึ่งทำงานกับ endpoints ที่เข้ากันได้กับ OpenAI เช่น GitHub Models

**AiServices** - สร้างอินเทอร์เฟซบริการ AI ที่ปลอดภัยตามประเภท กำหนดเมธอด ติดแอนโนเทชันด้วย `@Tool` และ LangChain4j จะจัดการการประสานงาน AI จะเรียกเมธอด Java ของคุณโดยอัตโนมัติเมื่อจำเป็น

**MessageWindowChatMemory** - รักษาประวัติการสนทนา หากไม่มีสิ่งนี้แต่ละคำขอจะเป็นอิสระ แต่ถ้ามี AI จะจดจำข้อความก่อนหน้าและรักษาบริบทข้ามหลายรอบ

<img src="../../../translated_images/architecture.eedc993a1c576839.th.png" alt="สถาปัตยกรรม LangChain4j" width="800"/>

*สถาปัตยกรรม LangChain4j - ส่วนประกอบหลักทำงานร่วมกันเพื่อขับเคลื่อนแอป AI ของคุณ*

## การพึ่งพา LangChain4j

การเริ่มต้นอย่างรวดเร็วนี้ใช้การพึ่งพา Maven สองตัวใน [`pom.xml`](../../../00-quick-start/pom.xml):

```xml
<!-- Core LangChain4j library -->
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j</artifactId> <!-- Inherited from BOM in root pom.xml -->
</dependency>

<!-- OpenAI integration (works with GitHub Models) -->
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-open-ai-official</artifactId> <!-- Inherited from BOM in root pom.xml -->
</dependency>
```

โมดูล `langchain4j-open-ai-official` ให้คลาส `OpenAiOfficialChatModel` ที่เชื่อมต่อกับ API ที่เข้ากันได้กับ OpenAI GitHub Models ใช้รูปแบบ API เดียวกัน ดังนั้นจึงไม่ต้องมีอะแดปเตอร์พิเศษ — เพียงแค่ชี้ base URL ไปที่ `https://models.github.ai/inference`

## ข้อกำหนดเบื้องต้น

**ใช้ Dev Container?** Java และ Maven ติดตั้งไว้แล้ว คุณแค่ต้องมี GitHub Personal Access Token

**พัฒนาในเครื่อง:**
- Java 21+, Maven 3.9+
- GitHub Personal Access Token (คำแนะนำด้านล่าง)

> **หมายเหตุ:** โมดูลนี้ใช้ `gpt-4.1-nano` จาก GitHub Models ห้ามแก้ไขชื่อโมเดลในโค้ด — ตั้งค่าให้ทำงานกับโมเดลที่ GitHub มีให้แล้ว

## การตั้งค่า

### 1. รับโทเค็น GitHub ของคุณ

1. ไปที่ [GitHub Settings → Personal Access Tokens](https://github.com/settings/personal-access-tokens)
2. คลิก "Generate new token"
3. ตั้งชื่อที่อธิบายได้ (เช่น "LangChain4j Demo")
4. ตั้งวันหมดอายุ (แนะนำ 7 วัน)
5. ใน "Account permissions" หา "Models" และตั้งเป็น "Read-only"
6. คลิก "Generate token"
7. คัดลอกและบันทึกโทเค็นของคุณ — คุณจะไม่เห็นอีก

### 2. ตั้งค่าโทเค็นของคุณ

**ตัวเลือก 1: ใช้ VS Code (แนะนำ)**

ถ้าคุณใช้ VS Code ให้เพิ่มโทเค็นของคุณในไฟล์ `.env` ที่โฟลเดอร์โปรเจกต์หลัก:

ถ้าไฟล์ `.env` ไม่มี ให้คัดลอก `.env.example` เป็น `.env` หรือสร้างไฟล์ `.env` ใหม่ในโฟลเดอร์โปรเจกต์หลัก

**ตัวอย่างไฟล์ `.env`:**
```bash
# ใน /workspaces/LangChain4j-for-Beginners/.env
GITHUB_TOKEN=your_token_here
```

จากนั้นคุณสามารถคลิกขวาที่ไฟล์ตัวอย่างใดก็ได้ (เช่น `BasicChatDemo.java`) ใน Explorer แล้วเลือก **"Run Java"** หรือใช้การตั้งค่า launch จากแผง Run and Debug

**ตัวเลือก 2: ใช้ Terminal**

ตั้งค่าโทเค็นเป็นตัวแปรสภาพแวดล้อม:

**Bash:**
```bash
export GITHUB_TOKEN=your_token_here
```

**PowerShell:**
```powershell
$env:GITHUB_TOKEN=your_token_here
```

## รันตัวอย่าง

**ใช้ VS Code:** คลิกขวาที่ไฟล์ตัวอย่างใดก็ได้ใน Explorer แล้วเลือก **"Run Java"** หรือใช้การตั้งค่า launch จากแผง Run and Debug (ตรวจสอบว่าคุณเพิ่มโทเค็นในไฟล์ `.env` แล้ว)

**ใช้ Maven:** หรือคุณสามารถรันจากบรรทัดคำสั่ง:

### 1. แชทพื้นฐาน

**Bash:**
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```

**PowerShell:**
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```

### 2. รูปแบบคำสั่ง

**Bash:**
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.PromptEngineeringDemo
```

**PowerShell:**
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.PromptEngineeringDemo
```

แสดงการ prompt แบบ zero-shot, few-shot, chain-of-thought และ role-based

### 3. การเรียกฟังก์ชัน

**Bash:**
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ToolIntegrationDemo
```

**PowerShell:**
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.ToolIntegrationDemo
```

AI จะเรียกเมธอด Java ของคุณโดยอัตโนมัติเมื่อจำเป็น

### 4. ถามตอบเอกสาร (RAG)

**Bash:**
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.SimpleReaderDemo
```

**PowerShell:**
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.SimpleReaderDemo
```

ถามคำถามเกี่ยวกับเนื้อหาใน `document.txt`

## สิ่งที่แต่ละตัวอย่างแสดง

**แชทพื้นฐาน** - [BasicChatDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/BasicChatDemo.java)

เริ่มที่นี่เพื่อดู LangChain4j ในรูปแบบที่ง่ายที่สุด คุณจะสร้าง `OpenAiOfficialChatModel` ส่ง prompt ด้วย `.chat()` และรับคำตอบกลับ นี่แสดงพื้นฐาน: วิธีเริ่มต้นโมเดลด้วย endpoints และ API keys ที่กำหนดเอง เมื่อเข้าใจรูปแบบนี้แล้ว ทุกอย่างอื่นจะสร้างต่อจากนี้

```java
ChatLanguageModel model = OpenAiOfficialChatModel.builder()
    .baseUrl("https://models.github.ai/inference")
    .apiKey(System.getenv("GITHUB_TOKEN"))
    .modelName("gpt-4.1-nano")
    .build();

String response = model.chat("What is LangChain4j?");
System.out.println(response);
```

> **🤖 ลองใช้กับ [GitHub Copilot](https://github.com/features/copilot) Chat:** เปิด [`BasicChatDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/BasicChatDemo.java) และถาม:
> - "ฉันจะเปลี่ยนจาก GitHub Models เป็น Azure OpenAI ในโค้ดนี้ได้อย่างไร?"
> - "มีพารามิเตอร์อื่นใดที่ฉันสามารถตั้งค่าใน OpenAiOfficialChatModel.builder() ได้บ้าง?"
> - "ฉันจะเพิ่มการตอบสนองแบบสตรีมมิ่งแทนการรอคำตอบสมบูรณ์ได้อย่างไร?"

**การออกแบบคำสั่ง (Prompt Engineering)** - [PromptEngineeringDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/PromptEngineeringDemo.java)

ตอนนี้ที่คุณรู้วิธีพูดกับโมเดลแล้ว มาดูสิ่งที่คุณพูดกับมัน ตัวอย่างนี้ใช้การตั้งค่าโมเดลเดียวกันแต่แสดงรูปแบบ prompt สี่แบบ ลอง zero-shot สำหรับคำสั่งตรงๆ, few-shot ที่เรียนรู้จากตัวอย่าง, chain-of-thought ที่แสดงขั้นตอนการคิด และ role-based ที่ตั้งบริบท คุณจะเห็นว่าโมเดลเดียวกันให้ผลลัพธ์แตกต่างกันอย่างมากขึ้นอยู่กับวิธีที่คุณตั้งคำขอ

```java
PromptTemplate template = PromptTemplate.from(
    "What's the best time to visit {{destination}} for {{activity}}?"
);

Prompt prompt = template.apply(Map.of(
    "destination", "Paris",
    "activity", "sightseeing"
));

String response = model.chat(prompt.text());
```

> **🤖 ลองใช้กับ [GitHub Copilot](https://github.com/features/copilot) Chat:** เปิด [`PromptEngineeringDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/PromptEngineeringDemo.java) และถาม:
> - "ความแตกต่างระหว่าง zero-shot กับ few-shot prompting คืออะไร และควรใช้เมื่อไหร่?"
> - "พารามิเตอร์ temperature มีผลต่อคำตอบของโมเดลอย่างไร?"
> - "มีเทคนิคอะไรบ้างเพื่อป้องกันการโจมตี prompt injection ในการใช้งานจริง?"
> - "ฉันจะสร้าง PromptTemplate ที่ใช้ซ้ำได้สำหรับรูปแบบทั่วไปอย่างไร?"

**การรวมเครื่องมือ (Tool Integration)** - [ToolIntegrationDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ToolIntegrationDemo.java)

นี่คือจุดที่ LangChain4j มีพลัง คุณจะใช้ `AiServices` เพื่อสร้างผู้ช่วย AI ที่สามารถเรียกเมธอด Java ของคุณได้ เพียงแค่ติดแอนโนเทชันเมธอดด้วย `@Tool("คำอธิบาย")` และ LangChain4j จะจัดการส่วนที่เหลือ — AI จะตัดสินใจใช้เครื่องมือแต่ละตัวตามคำถามของผู้ใช้โดยอัตโนมัติ นี่แสดงการเรียกฟังก์ชัน เทคนิคสำคัญสำหรับการสร้าง AI ที่สามารถทำงาน ไม่ใช่แค่ตอบคำถาม

```java
@Tool("Performs addition of two numeric values")
public double add(double a, double b) {
    return a + b;
}

MathAssistant assistant = AiServices.create(MathAssistant.class, model);
String response = assistant.chat("What is 25 plus 17?");
```

> **🤖 ลองใช้กับ [GitHub Copilot](https://github.com/features/copilot) Chat:** เปิด [`ToolIntegrationDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/ToolIntegrationDemo.java) และถาม:
> - "แอนโนเทชัน @Tool ทำงานอย่างไร และ LangChain4j ทำอะไรกับมันเบื้องหลัง?"
> - "AI สามารถเรียกใช้เครื่องมือหลายตัวต่อเนื่องกันเพื่อแก้ปัญหาซับซ้อนได้ไหม?"
> - "ถ้าเครื่องมือเกิดข้อผิดพลาด ฉันควรจัดการอย่างไร?"
> - "ฉันจะรวม API จริงแทนตัวอย่างเครื่องคิดเลขนี้ได้อย่างไร?"

**ถามตอบเอกสาร (RAG)** - [SimpleReaderDemo.java](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/SimpleReaderDemo.java)

ที่นี่คุณจะเห็นพื้นฐานของ RAG (retrieval-augmented generation) แทนที่จะพึ่งพาข้อมูลการฝึกของโมเดล คุณโหลดเนื้อหาจาก [`document.txt`](../../../00-quick-start/document.txt) และใส่ใน prompt AI จะตอบตามเอกสารของคุณ ไม่ใช่ความรู้ทั่วไป นี่คือก้าวแรกสู่การสร้างระบบที่ทำงานกับข้อมูลของคุณเอง

```java
Document document = FileSystemDocumentLoader.loadDocument("document.txt");
String content = document.text();

String prompt = "Based on this document: " + content + 
                "\nQuestion: What is the main topic?";
String response = model.chat(prompt);
```

> **หมายเหตุ:** วิธีง่ายๆ นี้โหลดเอกสารทั้งหมดเข้าไปใน prompt สำหรับไฟล์ใหญ่ (>10KB) คุณจะเกินขีดจำกัดบริบท โมดูล 03 จะครอบคลุมการแบ่งชิ้นและการค้นหาเวกเตอร์สำหรับระบบ RAG ในการผลิต

> **🤖 ลองใช้กับ [GitHub Copilot](https://github.com/features/copilot) Chat:** เปิด [`SimpleReaderDemo.java`](../../../00-quick-start/src/main/java/com/example/langchain4j/quickstart/SimpleReaderDemo.java) และถาม:
> - "RAG ป้องกัน AI สร้างข้อมูลผิดพลาดอย่างไรเมื่อเทียบกับการใช้ข้อมูลฝึกของโมเดล?"
> - "ความแตกต่างระหว่างวิธีง่ายๆ นี้กับการใช้เวกเตอร์ embeddings สำหรับการค้นหาคืออะไร?"
> - "ฉันจะขยายระบบนี้ให้รองรับเอกสารหลายฉบับหรือฐานความรู้ขนาดใหญ่ได้อย่างไร?"
> - "แนวทางปฏิบัติที่ดีที่สุดสำหรับการจัดโครงสร้าง prompt เพื่อให้ AI ใช้บริบทที่ให้มาเท่านั้นคืออะไร?"

## การดีบัก

ตัวอย่างมี `.logRequests(true)` และ `.logResponses(true)` เพื่อแสดงการเรียก API ในคอนโซล ช่วยแก้ไขปัญหาการตรวจสอบสิทธิ์ ขีดจำกัดอัตรา หรือคำตอบที่ไม่คาดคิด ลบธงเหล่านี้ในระบบจริงเพื่อลดเสียงรบกวนในล็อก

## ขั้นตอนถัดไป

**โมดูลถัดไป:** [01-introduction - เริ่มต้นกับ LangChain4j และ gpt-5 บน Azure](../01-introduction/README.md)

---

**การนำทาง:** [← กลับไปที่หลัก](../README.md) | [ถัดไป: Module 01 - บทนำ →](../01-introduction/README.md)

---

## การแก้ไขปัญหา

### การสร้าง Maven ครั้งแรก

**ปัญหา:** `mvn clean compile` หรือ `mvn package` ครั้งแรกใช้เวลานาน (10-15 นาที)

**สาเหตุ:** Maven ต้องดาวน์โหลดการพึ่งพาทั้งหมดของโปรเจกต์ (Spring Boot, ไลบรารี LangChain4j, SDK ของ Azure ฯลฯ) ในการสร้างครั้งแรก

**วิธีแก้:** นี่เป็นพฤติกรรมปกติ การสร้างครั้งถัดไปจะเร็วขึ้นมากเพราะมีการแคชการพึ่งพาไว้แล้ว เวลาดาวน์โหลดขึ้นกับความเร็วเครือข่ายของคุณ

### ไวยากรณ์คำสั่ง Maven บน PowerShell

**ปัญหา:** คำสั่ง Maven ล้มเหลวพร้อมข้อผิดพลาด `Unknown lifecycle phase ".mainClass=..."`

**สาเหตุ:** PowerShell แปล `=` เป็นตัวกำหนดตัวแปร ทำให้ไวยากรณ์ property ของ Maven ผิดพลาด

**วิธีแก้:** ใช้ตัวหยุดการแยกวิเคราะห์ `--%` ก่อนคำสั่ง Maven:

**PowerShell:**
```powershell
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```

**Bash:**
```bash
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.quickstart.BasicChatDemo
```

ตัวดำเนินการ `--%` บอก PowerShell ให้ส่งอาร์กิวเมนต์ที่เหลือทั้งหมดไปยัง Maven โดยตรงโดยไม่แปลความหมาย

### การแสดงผลอีโมจิใน Windows PowerShell

**ปัญหา:** คำตอบ AI แสดงตัวอักษรผิดปกติ (เช่น `????` หรือ `â??`) แทนอีโมจิใน PowerShell

**สาเหตุ:** การเข้ารหัสเริ่มต้นของ PowerShell ไม่รองรับอีโมจิ UTF-8

**วิธีแก้:** รันคำสั่งนี้ก่อนรันแอป Java:
```cmd
chcp 65001
```

คำสั่งนี้บังคับให้ใช้การเข้ารหัส UTF-8 ในเทอร์มินัล หรือใช้ Windows Terminal ซึ่งรองรับ Unicode ดีกว่า

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**ข้อจำกัดความรับผิดชอบ**:  
เอกสารนี้ได้รับการแปลโดยใช้บริการแปลภาษาอัตโนมัติ [Co-op Translator](https://github.com/Azure/co-op-translator) แม้เราจะพยายามให้ความถูกต้องสูงสุด แต่โปรดทราบว่าการแปลอัตโนมัติอาจมีข้อผิดพลาดหรือความไม่ถูกต้อง เอกสารต้นฉบับในภาษาต้นทางถือเป็นแหล่งข้อมูลที่เชื่อถือได้ สำหรับข้อมูลที่สำคัญ ขอแนะนำให้ใช้บริการแปลโดยผู้เชี่ยวชาญมนุษย์ เราไม่รับผิดชอบต่อความเข้าใจผิดหรือการตีความผิดใด ๆ ที่เกิดจากการใช้การแปลนี้
<!-- CO-OP TRANSLATOR DISCLAIMER END -->