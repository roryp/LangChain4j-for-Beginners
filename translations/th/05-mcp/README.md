<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "f89f4c106d110e4943c055dd1a2f1dff",
  "translation_date": "2025-12-31T01:02:24+00:00",
  "source_file": "05-mcp/README.md",
  "language_code": "th"
}
-->
# โมดูล 05: โปรโตคอลบริบทของโมเดล (MCP)

## สารบัญ

- [สิ่งที่คุณจะได้เรียนรู้](../../../05-mcp)
- [MCP คืออะไร?](../../../05-mcp)
- [MCP ทำงานอย่างไร](../../../05-mcp)
- [โมดูล Agentic](../../../05-mcp)
- [การรันตัวอย่าง](../../../05-mcp)
  - [ข้อกำหนดเบื้องต้น](../../../05-mcp)
- [เริ่มต้นอย่างรวดเร็ว](../../../05-mcp)
  - [การทำงานกับไฟล์ (Stdio)](../../../05-mcp)
  - [เอเยนต์ผู้ควบคุม (Supervisor Agent)](../../../05-mcp)
    - [การทำความเข้าใจกับผลลัพธ์](../../../05-mcp)
    - [คำอธิบายฟีเจอร์ของโมดูล Agentic](../../../05-mcp)
- [แนวคิดสำคัญ](../../../05-mcp)
- [ขอแสดงความยินดี!](../../../05-mcp)
  - [ต่อไปทำอะไร?](../../../05-mcp)

## สิ่งที่คุณจะได้เรียนรู้

คุณได้สร้าง AI สำหรับการสนทนา เรียนรู้การออกแบบพรอมต์ ทำให้คำตอบอ้างอิงกับเอกสาร และสร้างเอเยนต์พร้อมเครื่องมือแล้ว แต่เครื่องมือทั้งหมดนั้นถูกสร้างขึ้นเฉพาะสำหรับแอปพลิเคชันของคุณ ถ้าคุณสามารถให้ AI เข้าถึงระบบนิเวศของเครื่องมือที่เป็นมาตรฐาน ซึ่งใครๆ ก็สามารถสร้างและแชร์ได้ล่ะ? ในโมดูลนี้ คุณจะได้เรียนรู้วิธีทำเช่นนั้นด้วยโปรโตคอลบริบทของโมเดล (MCP) และโมดูล agentic ของ LangChain4j เราจะแสดงตัวอย่างตัวอ่านไฟล์ MCP แบบง่ายก่อน แล้วจึงแสดงให้เห็นว่ามันผสานเข้ากับเวิร์กโฟลว์ agentic ขั้นสูงโดยใช้รูปแบบ Supervisor Agent ได้อย่างไร

## MCP คืออะไร?

โปรโตคอลบริบทของโมเดล (MCP) ให้สิ่งนั้นโดยตรง — วิธีมาตรฐานสำหรับแอปพลิเคชัน AI ในการค้นหาและใช้เครื่องมือภายนอก แทนที่จะเขียนการผนวกรวมแบบกำหนดเองสำหรับแต่ละแหล่งข้อมูลหรือบริการ คุณเชื่อมต่อกับเซิร์ฟเวอร์ MCP ที่เปิดเผยความสามารถของพวกเขาในรูปแบบที่สอดคล้องกัน เอเยนต์ AI ของคุณจึงสามารถค้นหาและใช้เครื่องมือเหล่านี้โดยอัตโนมัติ

<img src="../../../translated_images/mcp-comparison.9129a881ecf10ff5.th.png" alt="การเปรียบเทียบ MCP" width="800"/>

*ก่อน MCP: การผนวกรวมแบบจุดต่อจุดที่ซับซ้อน หลัง MCP: โปรโตคอลเดียว โอกาสไม่รู้จบ.*

MCP แก้ปัญหาพื้นฐานในการพัฒนา AI: การผนวกรวมทุกชิ้นเป็นแบบกำหนดเอง ต้องการเข้าถึง GitHub? โค้ดสั่งทำพิเศษ ต้องการอ่านไฟล์? โค้ดสั่งทำพิเศษ ต้องการ query ฐานข้อมูล? โค้ดสั่งทำพิเศษ และการผนวกรวมเหล่านี้ไม่ทำงานร่วมกับแอปพลิเคชัน AI อื่นๆ

MCP ทำให้สิ่งนี้เป็นมาตรฐาน เซิร์ฟเวอร์ MCP เปิดเผยเครื่องมือต่างๆ พร้อมคำอธิบายและสคีมาใพารามิเตอร์ใดๆ ลูกค้า MCP ใดๆ ก็สามารถเชื่อมต่อ ค้นหาเครื่องมือที่ใช้งานได้ และใช้เครื่องมือเหล่านั้น สร้างครั้งเดียว ใช้ได้ทุกที่

<img src="../../../translated_images/mcp-architecture.b3156d787a4ceac9.th.png" alt="สถาปัตยกรรม MCP" width="800"/>

*สถาปัตยกรรมโปรโตคอลบริบทของโมเดล - การค้นหาและการเรียกใช้เครื่องมือที่เป็นมาตรฐาน*

## MCP ทำงานอย่างไร

**สถาปัตยกรรม เซิร์ฟเวอร์-ไคลเอนต์**

MCP ใช้รูปแบบเซิร์ฟเวอร์-ไคลเอนต์ เซิร์ฟเวอร์ให้บริการเครื่องมือ — อ่านไฟล์, สืบค้นฐานข้อมูล, เรียก API ไคลเอนต์ (แอปพลิเคชัน AI ของคุณ) เชื่อมต่อกับเซิร์ฟเวอร์และใช้เครื่องมือของพวกเขา

เพื่อใช้ MCP กับ LangChain4j ให้เพิ่มการพึ่งพา Maven ต่อไปนี้:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-mcp</artifactId>
    <version>${langchain4j.version}</version>
</dependency>
```

**การค้นหาเครื่องมือ**

เมื่อไคลเอนต์ของคุณเชื่อมต่อกับเซิร์ฟเวอร์ MCP มันจะถามว่า "คุณมีเครื่องมืออะไรบ้าง?" เซิร์ฟเวอร์ตอบกลับด้วยรายการเครื่องมือที่มี แต่ละตัวมีคำอธิบายและสคีมาพารามิเตอร์ เอเยนต์ AI ของคุณจึงสามารถตัดสินใจได้ว่าจะใช้เครื่องมือใดตามคำขอของผู้ใช้

**กลไกการส่งข้อมูล**

MCP สนับสนุนกลไกการส่งข้อมูลที่แตกต่างกัน โมดูลนี้สาธิตการส่งข้อมูลแบบ Stdio สำหรับกระบวนการในเครื่อง:

<img src="../../../translated_images/transport-mechanisms.2791ba7ee93cf020.th.png" alt="กลไกการส่งข้อมูลของ MCP" width="800"/>

*MCP กลไกการส่งข้อมูล: HTTP สำหรับเซิร์ฟเวอร์ระยะไกล, Stdio สำหรับกระบวนการในเครื่อง*

**Stdio** - [StdioTransportDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java)

สำหรับกระบวนการในเครื่อง แอปพลิเคชันของคุณจะสร้างเซิร์ฟเวอร์เป็น subprocess และสื่อสารผ่าน standard input/output มีประโยชน์สำหรับการเข้าถึงระบบไฟล์หรือเครื่องมือบรรทัดคำสั่ง

```java
McpTransport stdioTransport = new StdioMcpTransport.Builder()
    .command(List.of(
        npmCmd, "exec",
        "@modelcontextprotocol/server-filesystem@2025.12.18",
        resourcesDir
    ))
    .logEvents(false)
    .build();
```

> **🤖 Try with [GitHub Copilot](https://github.com/features/copilot) Chat:** Open [`StdioTransportDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/StdioTransportDemo.java) and ask:
> - "Stdio transport ทำงานอย่างไร และควรใช้เมื่อใดเมื่อเทียบกับ HTTP?"
> - "LangChain4j จัดการวงจรชีวิตของกระบวนการเซิร์ฟเวอร์ MCP ที่ถูกสร้างขึ้นอย่างไร?"
> - "ผลกระทบด้านความปลอดภัยของการให้ AI เข้าถึงระบบไฟล์คืออะไร?"

## โมดูล Agentic

แม้ว่า MCP จะให้เครื่องมือที่เป็นมาตรฐาน โมดูล **agentic** ของ LangChain4j ให้วิธีการเชิงประกาศเพื่อสร้างเอเยนต์ที่ประสานเครื่องมือเหล่านั้นได้ การใส่ `@Agent` และ `AgenticServices` ช่วยให้คุณกำหนดพฤติกรรมของเอเยนต์ผ่านอินเทอร์เฟซ แทนการเขียนโค้ดเชิงคำสั่ง

ในโมดูลนี้ คุณจะสำรวจรูปแบบ **Supervisor Agent** — แนวทาง agentic ขั้นสูงที่เอเยนต์ "ผู้ควบคุม" ตัดสินใจแบบไดนามิกว่าจะเรียกใช้ซับเอเยนต์ใดตามคำขอของผู้ใช้ เราจะรวมทั้งสองแนวคิดโดยให้หนึ่งในซับเอเยนต์ของเรามีความสามารถในการเข้าถึงไฟล์ผ่าน MCP

เพื่อใช้โมดูล agentic ให้เพิ่มการพึ่งพา Maven ต่อไปนี้:

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-agentic</artifactId>
    <version>${langchain4j.mcp.version}</version>
</dependency>
```

> **⚠️ Experimental:** โมดูล `langchain4j-agentic` เป็น **รุ่นทดลอง** และอาจมีการเปลี่ยนแปลง วิธีที่เสถียรในการสร้างผู้ช่วย AI ยังคงเป็น `langchain4j-core` พร้อมเครื่องมือที่เขียนขึ้นเอง (โมดูล 04)

## การรันตัวอย่าง

### ข้อกำหนดเบื้องต้น

- Java 21+, Maven 3.9+
- Node.js 16+ และ npm (สำหรับเซิร์ฟเวอร์ MCP)
- ตัวแปรสภาพแวดล้อมถูกตั้งค่าในไฟล์ `.env` (จากไดเรกทอรีราก):
  - **สำหรับ StdioTransportDemo:** `GITHUB_TOKEN` (GitHub Personal Access Token)
  - **สำหรับ SupervisorAgentDemo:** `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_DEPLOYMENT` (เหมือนกับโมดูล 01-04)

> **หมายเหตุ:** หากคุณยังไม่ได้ตั้งค่าตัวแปรสภาพแวดล้อม ให้ดู [Module 00 - Quick Start](../00-quick-start/README.md) สำหรับคำแนะนำ หรือคัดลอก `.env.example` เป็น `.env` ในไดเรกทอรีรากแล้วกรอกค่าของคุณ

## เริ่มต้นอย่างรวดเร็ว

**การใช้ VS Code:** คลิกขวาที่ไฟล์ตัวอย่างใดก็ได้ใน Explorer แล้วเลือก **"Run Java"** หรือใช้การกำหนดค่า launch จากแผง Run and Debug (ตรวจสอบให้แน่ใจว่าคุณได้เพิ่มโทเค็นของคุณในไฟล์ `.env` ก่อน)

**การใช้ Maven:** อีกทางเลือกหนึ่ง คุณสามารถรันจากคอมมานด์ไลน์ด้วยตัวอย่างด้านล่าง

### การทำงานกับไฟล์ (Stdio)

นี่แสดงตัวอย่างเครื่องมือที่รันเป็น subprocess ในเครื่อง

**✅ ไม่ต้องมีข้อกำหนดล่วงหน้า** - เซิร์ฟเวอร์ MCP จะถูกสร้างขึ้นโดยอัตโนมัติ

**การใช้ VS Code:** คลิกขวาที่ `StdioTransportDemo.java` และเลือก **"Run Java"**

**การใช้ Maven:**

**Bash:**
```bash
export GITHUB_TOKEN=your_token_here
cd 05-mcp
mvn compile exec:java -Dexec.mainClass=com.example.langchain4j.mcp.StdioTransportDemo
```

**PowerShell:**
```powershell
$env:GITHUB_TOKEN=your_token_here
cd 05-mcp
mvn --% compile exec:java -Dexec.mainClass=com.example.langchain4j.mcp.StdioTransportDemo
```

แอปพลิเคชันจะสร้างเซิร์ฟเวอร์ MCP ของระบบไฟล์โดยอัตโนมัติและอ่านไฟล์ท้องถิ่น สังเกตว่าการจัดการ subprocess ถูกดูแลให้คุณเรียบร้อยแล้ว

**ผลลัพธ์ที่คาดว่าจะได้รับ:**
```
Assistant response: The file provides an overview of LangChain4j, an open-source Java library
for integrating Large Language Models (LLMs) into Java applications...
```

### เอเยนต์ผู้ควบคุม (Supervisor Agent)

<img src="../../../translated_images/agentic.cf84dcda226374e3.th.png" alt="โมดูล Agentic" width="800"/>


รูปแบบ **Supervisor Agent** เป็นรูปแบบ agentic ที่มีความยืดหยุ่น แตกต่างจากเวิร์กโฟลว์แบบตายตัว (เช่น ลำดับ, ลูป, ขนาน) Supervisor ใช้ LLM เพื่อ autonomously ตัดสินใจว่าเรียกใช้เอเยนต์ใดตามคำขอของผู้ใช้

**รวม Supervisor กับ MCP:** ในตัวอย่างนี้ เราให้ `FileAgent` เข้าถึงเครื่องมือระบบไฟล์ของ MCP ผ่าน `toolProvider(mcpToolProvider)` เมื่อผู้ใช้ขอให้ "อ่านและวิเคราะห์ไฟล์" Supervisor จะวิเคราะห์คำขอและสร้างแผนการดำเนินการ จากนั้นจะส่งคำขอนั้นไปยัง `FileAgent` ซึ่งใช้เครื่องมือ `read_file` ของ MCP เพื่อดึงเนื้อหา Supervisor ส่งต่อเนื้อหานั้นไปยัง `AnalysisAgent` เพื่อแปลความ และถ้าจำเป็นเรียก `SummaryAgent` เพื่อย่อตัวผลลัพธ์

นี่แสดงให้เห็นว่าเครื่องมือ MCP ผสานเข้ากับเวิร์กโฟลว์ agentic ได้อย่างไร — Supervisor ไม่จำเป็นต้องรู้ว่าการอ่านไฟล์ทำอย่างไร เพียงรู้ว่า `FileAgent` ทำได้ Supervisor ปรับตัวแบบไดนามิกตามคำขอชนิดต่างๆ และคืนค่าตอบกลับเป็นผลลัพธ์สุดท้ายของเอเยนต์ตัวสุดท้ายหรือสรุปของการดำเนินการทั้งหมด

**การใช้สคริปต์เริ่มต้น (แนะนำ):**

สคริปต์เริ่มต้นจะโหลดตัวแปรสภาพแวดล้อมจากไฟล์ `.env` ในรากโดยอัตโนมัติ:

**Bash:**
```bash
cd 05-mcp
chmod +x start.sh
./start.sh
```

**PowerShell:**
```powershell
cd 05-mcp
.\start.ps1
```

**การใช้ VS Code:** คลิกขวาที่ `SupervisorAgentDemo.java` และเลือก **"Run Java"** (ตรวจสอบให้แน่ใจว่าไฟล์ `.env` ถูกตั้งค่าแล้ว)

**การทำงานของ Supervisor:**

```java
// กำหนดเอเจนต์หลายตัวที่มีความสามารถเฉพาะ
FileAgent fileAgent = AgenticServices.agentBuilder(FileAgent.class)
        .chatModel(model)
        .toolProvider(mcpToolProvider)  // มีเครื่องมือ MCP สำหรับการจัดการไฟล์
        .build();

AnalysisAgent analysisAgent = AgenticServices.agentBuilder(AnalysisAgent.class)
        .chatModel(model)
        .build();

SummaryAgent summaryAgent = AgenticServices.agentBuilder(SummaryAgent.class)
        .chatModel(model)
        .build();

// สร้าง Supervisor ที่ประสานงานเอเจนต์เหล่านี้
SupervisorAgent supervisor = AgenticServices.supervisorBuilder()
        .chatModel(model)  // โมเดล "planner"
        .subAgents(fileAgent, analysisAgent, summaryAgent)
        .responseStrategy(SupervisorResponseStrategy.SUMMARY)
        .build();

// Supervisor ตัดสินใจโดยอัตโนมัติว่าจะเรียกใช้เอเจนต์ใด
// เพียงส่งคำขอเป็นภาษาธรรมชาติ - LLM จะวางแผนการดำเนินการ
String response = supervisor.invoke("Read the file at /path/file.txt and analyze it");
```

ดู [SupervisorAgentDemo.java](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) สำหรับการใช้งานแบบสมบูรณ์

> **🤖 Try with [GitHub Copilot](https://github.com/features/copilot) Chat:** Open [`SupervisorAgentDemo.java`](../../../05-mcp/src/main/java/com/example/langchain4j/mcp/SupervisorAgentDemo.java) and ask:
> - "Supervisor ตัดสินใจเรียกเอเยนต์ใดอย่างไร?"
> - "ความแตกต่างระหว่างรูปแบบ Supervisor กับรูปแบบเวิร์กโฟลว์แบบ Sequential คืออะไร?"
> - "ฉันปรับพฤติกรรมการวางแผนของ Supervisor ได้อย่างไร?"

#### การทำความเข้าใจกับผลลัพธ์

เมื่อคุณรันเดโม คุณจะเห็นการเดินงานที่มีโครงสร้างแสดงว่าตัว Supervisor ประสานงานเอเยนต์หลายตัวอย่างไร นี่คือความหมายของแต่ละส่วน:

```
======================================================================
  SUPERVISOR AGENT DEMO
======================================================================

This demo shows how a Supervisor Agent orchestrates multiple specialized agents.
The Supervisor uses an LLM to decide which agent to call based on the task.
```

**หัวเรื่อง** แนะนำเดโมและอธิบายแนวคิดหลัก: Supervisor ใช้ LLM (ไม่ใช่กฎที่เขียนตายตัว) ในการตัดสินใจว่าเรียกเอเยนต์ใด

```
--- AVAILABLE AGENTS -------------------------------------------------
  [FILE]     FileAgent     - Reads files using MCP filesystem tools
  [ANALYZE]  AnalysisAgent - Analyzes content for structure, tone, and themes
  [SUMMARY]  SummaryAgent  - Creates concise summaries of content
```

**เอเยนต์ที่ใช้งานได้** แสดงเอเยนต์เฉพาะทางสามตัวที่ Supervisor สามารถเลือกใช้ แต่ละเอเยนต์มีความสามารถเฉพาะ:
- **FileAgent** สามารถอ่านไฟล์โดยใช้เครื่องมือของ MCP (ความสามารถภายนอก)
- **AnalysisAgent** วิเคราะห์เนื้อหา (ความสามารถโดย LLM)
- **SummaryAgent** สร้างสรุป (ความสามารถโดย LLM)

```
--- USER REQUEST -----------------------------------------------------
  "Read the file at .../file.txt and analyze what it's about"
```

**คำขอผู้ใช้** แสดงสิ่งที่ถูกถาม Supervisor ต้องแยกวิเคราะห์คำถามนี้และตัดสินใจว่าเรียกเอเยนต์ใด

```
--- SUPERVISOR ORCHESTRATION -----------------------------------------
  The Supervisor will now decide which agents to invoke and in what order...

  +-- STEP 1: Supervisor chose -> FileAgent (reading file via MCP)
  |
  |   Input: .../file.txt
  |
  |   Result: LangChain4j is an open-source Java library designed to simplify...
  +-- [OK] FileAgent (reading file via MCP) completed

  +-- STEP 2: Supervisor chose -> AnalysisAgent (analyzing content)
  |
  |   Input: LangChain4j is an open-source Java library...
  |
  |   Result: Structure: The content is organized into clear paragraphs that int...
  +-- [OK] AnalysisAgent (analyzing content) completed
```

**การประสานงานของ Supervisor** คือส่วนที่เวทมนตร์เกิดขึ้น ดูว่า:
1. Supervisor **เลือก FileAgent เป็นอันดับแรก** เพราะคำขอระบุ "อ่านไฟล์"
2. FileAgent ใช้เครื่องมือ `read_file` ของ MCP เพื่อดึงเนื้อหาไฟล์
3. Supervisor จากนั้น **เลือก AnalysisAgent** และส่งเนื้อหาไฟล์ให้มัน
4. AnalysisAgent วิเคราะห์โครงสร้าง น้ำเสียง และธีมต่างๆ

สังเกตว่า Supervisor ตัดสินใจเหล่านี้ **โดยอัตโนมัติ** ตามคำขอของผู้ใช้ — ไม่มีเวิร์กโฟลว์ที่เขียนตายตัว!

**คำตอบสุดท้าย** เป็นคำตอบที่ Supervisor สังเคราะห์ โดยรวมผลลัพธ์จากเอเยนต์ทั้งหมดที่ถูกเรียกใช้งาน ตัวอย่างจะแสดง agentic scope ซึ่งแสดงสรุปและผลการวิเคราะห์ที่แต่ละเอเยนต์เก็บไว้

```
--- FINAL RESPONSE ---------------------------------------------------
I read the contents of the file and analyzed its structure, tone, and key themes.
The file introduces LangChain4j as an open-source Java library for integrating
large language models...

--- AGENTIC SCOPE (Shared Memory) ------------------------------------
  Agents store their results in a shared scope for other agents to use:
  * summary: LangChain4j is an open-source Java library...
  * analysis: Structure: The content is organized into clear paragraphs that in...
```

### คำอธิบายฟีเจอร์ของโมดูล Agentic

ตัวอย่างแสดงฟีเจอร์ขั้นสูงหลายอย่างของโมดูล agentic มาดู Agentic Scope และ Agent Listeners ให้ละเอียดขึ้น

**Agentic Scope** แสดงหน่วยความจำที่ใช้ร่วมกันซึ่งเอเยนต์เก็บผลลัพธ์ของพวกเขาโดยใช้ `@Agent(outputKey="...")` สิ่งนี้อนุญาตให้:
- เอเยนต์ในภายหลังเข้าถึงผลลัพธ์ของเอเยนต์ก่อนหน้า
- Supervisor สังเคราะห์คำตอบสุดท้าย
- คุณตรวจสอบได้ว่าแต่ละเอเยนต์ผลิตอะไรบ้าง

```java
ResultWithAgenticScope<String> result = supervisor.invokeWithAgenticScope(request);
AgenticScope scope = result.agenticScope();
String story = scope.readState("story");
List<AgentInvocation> history = scope.agentInvocations("analysisAgent");
```

**Agent Listeners** ช่วยให้สามารถมอนิเตอร์และดีบักการทำงานของเอเยนต์ได้ ผลลัพธ์ทีละขั้นตอนที่คุณเห็นในเดโมมาจาก AgentListener ที่เชื่อมต่อกับการเรียกใช้เอเยนต์แต่ละครั้ง:
- **beforeAgentInvocation** - ถูกเรียกเมื่อ Supervisor เลือกเอเยนต์ ให้คุณเห็นว่าเอเยนต์ใดถูกเลือกและเพราะเหตุใด
- **afterAgentInvocation** - ถูกเรียกเมื่อเอเยนต์เสร็จสิ้น แสดงผลลัพธ์ของมัน
- **inheritedBySubagents** - เมื่อเป็นจริง listener จะมอนิเตอร์เอเยนต์ทั้งหมดในลำดับชั้น

```java
AgentListener monitor = new AgentListener() {
    private int step = 0;
    
    @Override
    public void beforeAgentInvocation(AgentRequest request) {
        step++;
        System.out.println("  +-- STEP " + step + ": " + request.agentName());
    }
    
    @Override
    public void afterAgentInvocation(AgentResponse response) {
        System.out.println("  +-- [OK] " + response.agentName() + " completed");
    }
    
    @Override
    public boolean inheritedBySubagents() {
        return true; // ส่งต่อไปยังตัวแทนย่อยทั้งหมด
    }
};
```

นอกจากรูปแบบ Supervisor แล้ว โมดูล `langchain4j-agentic` ยังให้รูปแบบเวิร์กโฟลว์และฟีเจอร์ที่ทรงพลังหลายอย่าง:

| รูปแบบ | คำอธิบาย | กรณีการใช้งาน |
|---------|-------------|----------|
| **Sequential** | ดำเนินการเอเยนต์ตามลำดับ ผลลัพธ์ไหลไปยังขั้นตอนถัดไป | ท่อข้อมูล: วิจัย → วิเคราะห์ → รายงาน |
| **Parallel** | รันเอเยนต์พร้อมกัน | งานอิสระ: สภาพอากาศ + ข่าว + หุ้น |
| **Loop** | ทำซ้ำจนกว่าจะถึงเงื่อนไขที่กำหนด | การให้คะแนนคุณภาพ: ปรับปรุงจนกว่าคะแนน ≥ 0.8 |
| **Conditional** | นำทางตามเงื่อนไข | แยกประเภท → ส่งต่อไปยังเอเยนต์เฉพาะทาง |
| **Human-in-the-Loop** | เพิ่มจุดตรวจสอบของมนุษย์ | เวิร์กโฟลว์การอนุมัติ, การตรวจเนื้อหา |

## แนวคิดสำคัญ

**MCP** เหมาะเมื่อคุณต้องการใช้ประโยชน์จากระบบนิเวศของเครื่องมือที่มีอยู่ สร้างเครื่องมือที่หลายแอปพลิเคชันสามารถแชร์ได้ ผนวกรวมบริการของบุคคลที่สามด้วยโปรโตคอลมาตรฐาน หรือสลับการใช้งานเครื่องมือโดยไม่ต้องเปลี่ยนโค้ด

**โมดูล Agentic** เหมาะที่สุดเมื่อคุณต้องการการกำหนดเอเยนต์เชิงประกาศด้วยการใส่ `@Agent` ต้องการการประสานเวิร์กโฟลว์ (ลำดับ, ลูป, ขนาน) ชอบการออกแบบเอเยนต์แบบอินเทอร์เฟซแทนโค้ดเชิงคำสั่ง หรือเมื่อคุณรวมเอเยนต์หลายตัวที่แชร์ผลลัพธ์ผ่าน `outputKey`

**รูปแบบ Supervisor Agent** โดดเด่นเมื่อเวิร์กโฟลว์ไม่สามารถคาดเดาล่วงหน้าและคุณต้องการให้ LLM เป็นผู้ตัดสินใจ เมื่อคุณมีเอเยนต์เฉพาะทางหลายตัวที่ต้องการการประสานแบบไดนามิก เมื่อสร้างระบบการสนทนาที่ต้องส่งคำขอไปยังความสามารถต่างๆ หรือเมื่อคุณต้องการพฤติกรรมเอเยนต์ที่ยืดหยุ่นและปรับตัวได้มากที่สุด

## ขอแสดงความยินดี!

คุณได้สำเร็จคอร์ส LangChain4j สำหรับผู้เริ่มต้นแล้ว คุณได้เรียนรู้:

- วิธีสร้าง AI สำหรับการสนทนาพร้อมหน่วยความจำ (โมดูล 01)
- แนวทางการออกแบบพรอมต์สำหรับงานต่างๆ (โมดูล 02)
- การทำให้คำตอบมีพื้นฐานจากเอกสารของคุณด้วย RAG (โมดูล 03)
- การสร้างเอเยนต์ AI พื้นฐาน (ผู้ช่วย) พร้อมเครื่องมือที่เขียนขึ้นเอง (โมดูล 04)
- การรวมเครื่องมือมาตรฐานเข้ากับโมดูล LangChain4j MCP และ Agentic (Module 05)

### ต่อไปคืออะไร?

หลังจากทำโมดูลเสร็จแล้ว ให้สำรวจ [คู่มือการทดสอบ](../docs/TESTING.md) เพื่อดูแนวคิดการทดสอบของ LangChain4j ในการปฏิบัติ

**แหล่งข้อมูลอย่างเป็นทางการ:**
- [เอกสาร LangChain4j](https://docs.langchain4j.dev/) - คู่มือโดยละเอียดและเอกสารอ้างอิง API
- [GitHub ของ LangChain4j](https://github.com/langchain4j/langchain4j) - ซอร์สโค้ดและตัวอย่าง
- [บทช่วยสอน LangChain4j](https://docs.langchain4j.dev/tutorials/) - บทช่วยสอนทีละขั้นตอนสำหรับกรณีการใช้งานต่างๆ

ขอบคุณที่เรียนจบหลักสูตรนี้!

---

**การนำทาง:** [← ก่อนหน้า: Module 04 - Tools](../04-tools/README.md) | [กลับไปยังหน้าหลัก](../README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
ข้อจำกัดความรับผิดชอบ:
เอกสารฉบับนี้ได้รับการแปลโดยใช้บริการแปลด้วยปัญญาประดิษฐ์ [Co-op Translator](https://github.com/Azure/co-op-translator) แม้ว่าเราจะพยายามให้การแปลมีความถูกต้อง โปรดทราบว่าการแปลโดยอัตโนมัติอาจมีข้อผิดพลาดหรือความคลาดเคลื่อนได้ เอกสารต้นฉบับในภาษาต้นทางควรถูกถือเป็นแหล่งข้อมูลที่เป็นหลัก สำหรับข้อมูลที่มีความสำคัญ ขอแนะนำให้ใช้การแปลโดยนักแปลมืออาชีพ เราไม่รับผิดชอบต่อความเข้าใจผิดหรือการตีความผิดพลาดใดๆ ที่เกิดขึ้นจากการใช้การแปลฉบับนี้
<!-- CO-OP TRANSLATOR DISCLAIMER END -->